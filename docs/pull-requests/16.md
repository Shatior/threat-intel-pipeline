# Pull request #16 — Fase 4 (bloque 1) — Especificación de los tres modos de informe, marca de agua e intervalo declarado

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T16:46:11Z |
| **Fusionado** | 2026-08-02T16:46:18Z |
| **Commit de fusión** | `ce309a8d54a746dc2a6413c16b7fdad6b2b2fa4c` |
| **Rama** | `claude/fase4-modos-informe` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/16 |
| **Comentarios archivados** | 0 |

## Descripción

Primero de los seis bloques de la fase 4. **Solo especificación**, salvo una desviación declarada abajo.

## El problema

El diferencial de §6 se define por comparación con el estado de la ejecución anterior. Sin ese estado, las dos salidas intuitivas son ambas inadmisibles: «0 indicadores nuevos» presenta una **ausencia de observación como observación de ausencia** (§14.3), y publicar los miles que devuelve la recolección presenta el **acumulado histórico como actividad del periodo**. No es un caso de arranque: ocurre en la primera ejecución, cada vez que el estado se pierde, y en cualquier despliegue futuro. Y el punto 3 de §13 exigía cobertura de «los tres modos de informe» cuando **ninguna sección los enumeraba**.

**Un informe sin estado anterior no es un informe diario defectuoso: es un producto de otro tipo.**

## Qué queda escrito

**§6** se reordena en siete subsecciones. Los **tres modos** con el modo fijado en dos instantes —candidato antes de recolectar, final tras la recolección, donde el fallo total prevalece—; **seis motivos de línea base declarados exhaustivos**; **vocabulario reservado** con alcance acotado para que la comprobación sea ejecutable; **marca de agua por fuente** y dos anclas temporales con su cometido separado; **techo de validez de los caídos** por fuente; umbrales de frescura con el de advertencia en 36 h y declarado *no medido*; regeneración cada 30 días evaluada por el pipeline; y transiciones.

**§8.3** lleva a la cabecera el modo, su motivo, el intervalo real, la línea base vigente y **qué cálculos no se publican y por qué**, y parte el panorama de técnicas: las derivadas se publican igual en ambos modos; las **inferidas se suprimen en línea base**, porque su denominador —las entradas KEV nuevas del periodo— no existe en un censo.

**§9** — el estado mínimo pasa de lista a objeto y gana los insumos que §6 exigía y no sostenía: marcas de agua por fuente, línea base vigente, las `fuentes` de cada indicador con su estado y marca de caída, y un bloque `kev` con los campos que el informe necesita tras un 304.

**§14.5** enumera **24 puntos de cobertura obligatoria de la fase 4**, que es lo que §13 punto 3 invoca por su nombre.

## Revisión — dieciséis pasadas, 178 hallazgos

Bloqueantes por pasada: **4, 3, 4, 2, 2, 2, 2, 1, 1, 2, 2, 1, 1, 1, 1, 0**. Total: 29 bloqueantes, 63 relevantes, 86 menores. Cada acta la escribió su revisor en `docs/revisiones/` y se commiteó sin modificar.

Lo más grave que encontraron: **el catálogo KEV entero publicado como caído**, que apareció por tres puertas distintas —el 304 que §5.2 declara el caso habitual, la clave de envoltura presente y vacía, y un lote entero de tipos no soportados—. La corrección definitiva dejó de enumerar caminos: una recolección observada sin indicadores **nunca** publica caídos. También: que la regla del 304 presuponía un contenido que el estado no guardaba; que el aplazamiento de un día `parcial` era pérdida, porque el validador condicional se guardaba igual; y que retirar `AttributeError` de un `except` convertía el descarte de un registro en la caída de la fuente entera.

**Siete veces** una regla escrita en varias sedes se corrigió solo donde se miraba. La respuesta final no fue propagar otra vez sino **consolidar**: la regla de la marca de agua vive en §6.4 y las demás secciones remiten.

## Desviación del alcance

§14.2 es fase 2, ya implementada, y dejarla afirmando lo contrario que el código habría sido la deriva que la regla 6 existe para evitar. Se tocan `cisa_kev.py`, `threatfox.py`, `base.py`, `cli.py` y `scripts/verificar_contratos.py`; todo verificado por mutación.

## Registro de métricas

Al llegar a veinte filas sonó su regla de retirada, la sesión implementadora **no subió el umbral ni la evaluó**, y el mantenedor decidió **conservarlo** con la evidencia que la propia regla exige. Umbral siguiente: 40 filas (entrada 24 de `docs/decisiones.md`).

**224 tests en verde**, `ruff` limpio, `verificar_contratos.py --sin-red` en 0.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
