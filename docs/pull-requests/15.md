# Pull request #15 — Verificador de contratos: retirada por identidad, camino verde y los cinco pendientes del PR #13

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T10:20:26Z |
| **Fusionado** | 2026-08-02T10:34:57Z |
| **Commit de fusión** | `fe1bc8064bbf9dec36b91e63d9ea420be62c69a0` |
| **Rama** | `claude/fase4-verificador-contratos` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/15 |
| **Comentarios archivados** | 0 |

## Descripción

PR pequeño e independiente. Cierra los pendientes que la tercera pasada del PR #13 dejó documentados, y sustituye la tercera comprobación de forma de §11.3 por verificación **por identidad**.

## R3-3 — la comprobación que no comprobaba nada

Contrastaba un recuento calculado como `objetos_software − objetos_software_vivos`, dos magnitudes que la barrera ya contrasta: la condición solo podía dispararse cuando la barrera ya había disparado. Y su mensaje afirmaba una causa —«un marcador desapareció»— que la condición no establecía.

**Pasa a verificación por identidad.** La línea base declara los tres objetos retirados con su marcador, **medidos descargando el bundle del commit fijado**: `Darkmoon` y `Ngrok` por `revoked`, `TRITON` por `x_mitre_deprecated`. Se comprueba que cada uno **sigue presente y sigue marcado**. (De paso, el digest del fichero descargado coincidió con el pin: queda confirmado contra la fuente viva, que era una limitación declarada por los tres revisores del PR #13.)

**Nunca igualdad del conjunto**, y es lo que decide el diseño: que MITRE retire un objeto más es evolución normal del catálogo, no una rotura de contrato. Exigir que sean exactamente estos tres convertiría cada deprecación futura en un rojo, y un rojo que suena por lo normal es la fatiga que §11.3 evita al separar «contrato roto» de «no verificado».

De ahí se sigue una restricción: **la lista debe cubrir los dos marcadores**, porque si todos los declarados se retiraran por el mismo, la desaparición del otro sería invisible.

## Los demás pendientes

**R3-1 / R3-5** — `UnicodeDecodeError` es subclase de `ValueError`, **no de `OSError`**, y escapaba a la captura del lector del pin: este fichero está lleno de comentarios con acentos, y basta un editor que guarde en latin-1 para que el canario semanal muera con traza en vez de declarar la laguna. `AttributeError` sale por inalcanzable. **R3-2** — modo `formato_roto`, que rompe solo la marca temporal y no sirve el bundle, de modo que el rojo lo decide esa mitad: antes la mutación `return ausentes` sobrevivía a la batería entera. **R3-4** — modo `conforme`: **primera vez que se ejecuta el camino verde** del verificador. **R-F** — modo `deriva_pin`: el aviso de commit nuevo, en sus dos direcciones, y en verde, porque el pin lo sube un humano (§5.5).

## Revisión — una pasada, sin bloqueantes, seis relevantes corregidos

El acta va en `docs/revisiones/` escrita por el revisor y commiteada sin modificar (`sha256` en el hilo). Lo que encontró, y que también corrige este PR:

- **Falso verde**: `objetos_retirados` no entró en las claves exigidas de la línea base, así que si faltaba la comprobación se saltaba y el script imprimía «forma verificados» en verde — la misma forma del defecto que venía a corregir.
- Una entrada malformada mataba el proceso, y la restricción de los dos marcadores se declaraba «contrato roto» cuando es un defecto de **nuestra** configuración: rotular un fallo propio como cambio de MITRE es la objeción que retiró la versión anterior. Ahora la validación es compartida por los dos caminos — el modo `--sin-red` daba por buena una lista vacía.
- La restricción de los dos marcadores **no tenía ninguna prueba**: borrarla dejaba la batería en verde.
- `URLError` en vez de `ErrorRed` llevaba el fichero de tests de 3,4 s a **35,1 s**, unos 30 s de `sleep` real duplicando cobertura que ya existe con reloj inyectado.
- `test_cada_acta_tiene_un_solo_commit` **no podía fallar en CI**: `checkout` clona con `fetch-depth: 1`. Corregido a historial completo.

## Congelamiento

Es el primer PR bajo el congelamiento del protocolo. Los **tres hallazgos de proceso** del revisor no se implementan: van a `docs/proceso-pendiente.md` como P-8 a P-10, fuera del recuento por severidad. Y contiene la primera aplicación de la excepción: `test_el_acta_no_comparte_commit_con_otros_cambios` fallaba en `main` por construcción —el squash junta el acta con todo el PR—, lo que **impedía aplicar** el protocolo; se retiró declarando qué garantía se pierde, y recuperarla quedó como P-7.

**206 tests en verde**, `ruff` limpio, `--sin-red` en 0. Todo verificado por mutación.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
