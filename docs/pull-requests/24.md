# Pull request #24 — Declara cerrada la fase 4 y anota la fila que faltaba del registro

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-03T03:33:05Z |
| **Fusionado** | 2026-08-03T03:39:07Z |
| **Commit de fusión** | `fd59e075192ec30263b12af8e9315ce18d5a29d1` |
| **Rama** | `claude/fase4-cierre-registro` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/24 |
| **Comentarios archivados** | 0 |

## Descripción

Cierre formal del hito de §13, con la evidencia de cada punto escrita en la propia sección.

| # | Evidencia |
|---|-----------|
| 1 | `python -m threatintel run` recorre el ciclo entero en una invocación |
| 2 | `2026-08-02` línea base (`estado_ausente`) → `2026-08-03` diferencial con **intervalo real de 3,7 h**. En producción, no en test |
| 3 | 449 tests en verde, los tres modos cubiertos de extremo a extremo |
| 4 | Dos informes en `reports/`, commiteados por `daily.yml` y fusionados en `main` |
| 5 | README rehecho, con `tests/test_readme.py` vigilando su contenido obligatorio |
| 6 | Barrido del historial completo sin hallazgos |

Con el cierre se levanta el congelamiento del protocolo y de la especificación, y se retira la restricción de no añadir funcionalidad nueva.

## La fila que faltaba, y lo que enseña

El revisor del PR #23 **no añadió su fila** al registro de métricas. La añade aquí la sesión implementadora, marcada `†` como manda la regla, porque un hueco mudo es indistinguible de «no hubo pasada».

El motivo es distinto de las cuatro veces anteriores y merece quedar escrito: **mi encargo no le pidió la fila.** Se le dio corpus acotado, presupuesto y salida esperada, y en esa salida no estaba. La regla existe en el protocolo; el encargo concreto no la trasladó.

Por lo mismo su columna de categorías va `n/d`: el encargo le hizo declarar cobertura contra una lista escrita a medida en lugar de contra la taxonomía numerada, de modo que **no consta** a qué categorías pertenecen sus hallazgos. Estimarlo sería inventar el dato que la columna existe para registrar.

Es la misma lección que produjo la pasada de dos horas: **lo que el encargo no pide, no ocurre**, por mucho que el protocolo lo mande.

Queda además anotado un hueco del propio registro: los PR #21 y #22 se fusionaron sin pasada de revisión, declarándolo en su hilo, y por tanto no tienen fila — pero entonces el registro no distingue «no hubo pasada» de «se olvidó la fila», que es la ambigüedad que esa regla quería cerrar.

**449 tests en verde.** Sin revisión independiente: es una declaración de estado y una fila de registro, no un cambio de comportamiento.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
