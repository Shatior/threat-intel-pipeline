# Pull request #23 — Cierra la fase 4: retira las marcas de pendiente, unifica el criterio de orden y rehace el README

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-03T03:21:08Z |
| **Fusionado** | 2026-08-03T03:24:29Z |
| **Commit de fusión** | `b947d2470e8c52ae72cdfccf9c5b018066b753a4` |
| **Rama** | `claude/fase4-cierre` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/23 |
| **Comentarios archivados** | 0 |

## Descripción

Bloque 6: verificación de los seis criterios de §13 contra el estado real y resolución de las once discrepancias que `CLAUDE.md` acumuló por el congelamiento.

## `CLAUDE.md`

**Tres marcas de «pendiente de implementación» describían código que llevaba días funcionando** —§11.2 llamaba pendiente a un workflow con dos informes ya en `main`, §9 decía que `persistencia.py` «escribe todavía una lista desnuda» cuando escribía el formato 2, §14.2 declaraba pendiente una carga implementada en el bloque anterior—. Se retiran.

**El árbol de §9 incorpora `analyze/estado.py`**, y **`validadores_http.json` entra con párrafo propio**: en un runner efímero, «conservar el `ETag` en `data/state/`» solo significa algo si el fichero se versiona. Sin eso, §5.2 llamaría «caso habitual» a un camino que el pipeline no recorrería jamás.

**El criterio de orden pasa a tener una sola sede.** §5.2 lo define —con la dirección justificada por la medición— y §8.3 remite.

**Cifras.** `producto_inespecifico` de 7,0% a 7,8%, los dos `~7%` aproximados a `~8%`, y la cobertura de la ruta B de 510 (30,8%) a **519 (31,3%)**, la de la ejecución real. La diferencia de nueve entradas **se declara no explicada**: había dos hipótesis y no se ha determinado cuál: sustituir la cifra en silencio habría convertido en confirmado lo que solo es más reciente.

## `docs/protocolo-revision.md`

**Presupuesto definitivo: 10 minutos y 30 mutaciones**, ahora con las pasadas medidas que lo sostienen —6 min/20, 7 min/5, 7,5 min/16, frente a 1 h 55 min y cero artefactos antes de acotar el encargo—. Ninguna agotó el tope.

**Convención nueva:** la marca de «pendiente» la retira el pull request que la satisface, y el revisor la comprueba en la categoría 7. La solución que se venía usando era correcta; su único fallo era depender de que alguien se acordara, y tres olvidos seguidos son una serie.

**Congelamiento levantado**, con su texto conservado como historia por la regla de §9.1.

## README

Rehecho entero. Afirmaba siete cosas falsas, todas prometiendo **menos** de lo que hay: «`run` aún no implementado», «`analyze/` y `report/` reservados, todavía no contienen lógica». La introducción queda como comentario para el mantenedor.

## Verificado en producción

**El 304 ocurrió de verdad.** La ejecución del 2026-08-03 registró `codigo_http: 304` sobre el validador que la anterior había commiteado. Hasta hoy §5.2 llamaba «caso habitual» a un camino que nadie había visto ocurrir.

Y el **criterio 2 de §13** quedó cumplido en producción, no en test: `2026-08-02` línea base por `estado_ausente` → `2026-08-03` diferencial con intervalo real de 3,7 h declarado.

## Revisión independiente

Una pasada, con el presupuesto nuevo. Acta íntegra en [`docs/revisiones/claude-fase4-cierre--pasada-1.md`](docs/revisiones/claude-fase4-cierre--pasada-1.md), commiteada sin modificar. **1 bloqueante, 2 relevantes, 1 menor — los cuatro corregidos.**

El **bloqueante** es el hallazgo que justifica el protocolo entero: el README rehecho suprimía la **escala de confianza** (§7, «documentada en el README») y la **sección de evaluación de fuentes** (§14.7, cinco puntos obligatorios por fuente), en el mismo commit que declara cerrado el hito cuyo punto 5 las exige. Con ellas desaparecían el riesgo de disponibilidad, los límites de tasa con suspensiones de hasta 72 h, la no exposición de IOCs de más de 6 meses y la degradación ante la caída de KEV y del bundle. Ambas vuelven en forma compacta.

El **relevante 1** es de la misma familia y más irónico: la viñeta que se declaraba «única sede» del criterio de orden dejaba viva, 36 líneas más arriba y **en su propia §5.2**, la redacción antigua.

El **relevante 2** es el que impide que se repita: **nada vigilaba el contenido obligatorio del README**, y el revisor lo demostró borrando una sección entera sin que muriera un solo test. Se añade `tests/test_readme.py`, que comprueba **presencia de contenido, no redacción**, para que sobreviva a las reescrituras.

Verificado por mutación:

| Mutación | Resultado |
|---|---|
| Borrar la escala de confianza | muere en `test_el_readme_publica_las_cuatro_bandas...` |
| Borrar la evaluación de fuentes *(el bloqueante)* | muere en `test_el_readme_cubre_los_puntos_obligatorios...` |
| Borrar la clave de abuse.ch *(la mutación que sobrevivió al revisor)* | muere en `test_el_readme_documenta_la_clave...` |
| Reinstalar el orden antiguo en `cli.py` | muere en `test_la_seccion_4_ordena_por_fecha_limite...` |

**449 tests en verde**, `ruff` limpio.

## Lo que este pull request deja abierto, y por qué

En `docs/proceso-pendiente.md`: los insumos de §8.2 sobre el catálogo (**E-1**) y la presentación consolidada de §6.1 (**E-2**), que son funcionalidad nueva y el punto final de §13 prohíbe añadirla antes de cerrar; y la cobertura de `reference` de ThreatFox (**E-3**), que se decide **midiendo tres ejecuciones más** en lugar de elegir ahora entre deriva del proveedor y variación. Bajar el umbral para que la alarma deje de sonar queda expresamente descartado.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
