# Pull request #12 — Instrumentación del protocolo de revisión y estatus de los artefactos documentales

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T07:26:03Z |
| **Fusionado** | 2026-08-02T07:45:03Z |
| **Commit de fusión** | `223dec637e4982589ae704b67657f77af50a9b29` |
| **Rama** | `claude/metricas-revision` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/12 |
| **Comentarios archivados** | 2 |

## Descripción

Solo documentación. Dos cosas: el protocolo de revisión pasa a medirse a sí mismo, y `CLAUDE.md` declara qué autoridad tiene cada documento del proyecto.

## Instrumentación del protocolo

El protocolo lleva funcionando desde el PR #9 **sin ningún dato sobre sí mismo**: cuántas pasadas hacen falta, qué categorías rinden, cuánto cuesta. Sus ajustes se han tomado por impresión, que es lo que el proyecto rechaza en el producto y no tenía por qué tolerar en el proceso.

**`docs/metricas-revision.md`** es un registro de pasadas: fecha, PR, fase, número de pasada, tipo de diff, duración, hallazgos por severidad y categorías en que cayeron. **La fila la anota el revisor al publicar su informe, en el mismo commit** —única excepción a la regla 2, declarada para no tener que decidirla en cada pasada—. Una fila reconstruida se marca con `†`, y una pasada **sin** fila no es una opción: un hueco mudo es indistinguible de «no hubo pasada», que es §14.3 trasladado al instrumento.

**Deliberadamente pobre**: una tabla, sin totales, sin medias y sin gráficos. Un agregado calculado se lee como una conclusión, y no hay todavía datos que sostengan ninguna.

**Las cuatro preguntas que debe responder** quedan escritas en el protocolo: en qué pasada dejan de aparecer bloqueantes; qué categorías rinden; si los diffs de documentación justifican el recorrido completo; y si el coste por defecto encontrado sube con el tiempo. **Ninguna decisión de calibración antes de acumular dos fases de datos.**

**Regla de retirada.** Si al cerrar la fase 4 el registro no ha servido para tomar ninguna decisión, se elimina. La retirada es el desenlace por defecto. Para que la regla pueda ejecutarse y no solo enunciarse: la juzga el mantenedor humano, con las entradas de `docs/decisiones.md` que citen el registro como expediente, y toda decisión tomada con él debe escribirse allí.

## Estatus de los artefactos documentales (§9.1)

El árbol de §9 no listaba `docs/` en absoluto pese a contener ya tres documentos, y cinco documentos gobernaban o describían el proyecto sin que su autoridad estuviera escrita en ninguna parte. Confundirlos permite que una decisión se cuele por la puerta de atrás: basta escribirla en el documento que nadie considera vinculante y citarla después como si lo fuera.

`CLAUDE.md` es fuente de verdad sobre el producto; el protocolo es normativo sobre el proceso; `docs/decisiones.md` es registro histórico que explica pero no manda; `docs/metricas-revision.md` es dato en bruto sin autoridad; `README.md` es derivado.

**Ante discrepancia en materia de producto prevalece `CLAUDE.md`** —el alcance va dentro de la regla, no como matiz posterior—, y **una entrada de decisiones superada por otra posterior sigue siendo válida como historia**: no se reescribe ni se borra. Una decisión revertida es el dato con más valor que el registro puede contener. De ahí el corolario: una entrada nunca se cita como autoridad de un comportamiento presente, solo para explicar cómo se llegó hasta aquí.

## Filas retroactivas

Diez filas, reconstruidas del historial de los PR #9, #10 y #11. **Solo lo verificable**; lo que no consta va como `n/d` y no se estima. Empieza en el #9 porque el protocolo se introdujo ahí —el #8 se fusionó a las 00:46 UTC y el primer informe es de las 00:57—.

Rellenarlas destapó un incumplimiento de la salida esperada del revisor: **los informes de las tres últimas pasadas del PR #11 nunca se publicaron como comentario**, solo las respuestas, de modo que sus menores y sus categorías no constan de forma exhaustiva. El protocolo pasa a exigir que el informe se publique en el hilo del PR.

## Revisión previa

Revisado por una sesión independiente antes de abrir el PR: **sin bloqueantes**, 11 relevantes y 6 menores, todos respondidos. El informe y la respuesta van en los comentarios. Los hallazgos de más peso, ya corregidos:

- La pasada 4 del PR #11 tenía **seis** relevantes, no cinco: su informe no lleva resumen y el sexto hallazgo vivía fuera de la secuencia numerada.
- Se declara la **regla de recuento** —se toma el resumen del informe; solo se cuenta a mano cuando no lo hay—, porque en la pasada 2 lo declarado (13/10) y lo enumerado (19/19) difieren en un 45%.
- **Columna «Fase» nueva**: las dos reglas de uso se enuncian en fases y la tabla no guardaba ese insumo. Es la comprobación de insumos del protocolo aplicada al instrumento que lo vigila, y ya había fallado una vez.
- La corrección del árbol de §9 dejaba intactas **dos incoherencias en el mismo árbol** —`scripts/verificar_contratos.py` sin declarar, y `.github/workflows/` declarando un fichero que no existe mientras existen cuatro que sí—, más el árbol gemelo del README, que además afirmaba que `enrich/` no contiene lógica. Categoría 10 en su forma literal. Corregido.

**190 tests en verde**, `ruff` limpio. Ningún cambio de comportamiento.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 2 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

---

**1. @vigiabref · 2026-08-02T07:27:32Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/12#issuecomment-5156205466 -->

# Revisión independiente — rama `claude/metricas-revision`, previa a abrir el PR

> **Nota de procedencia.** Este informe lo produjo una sesión de revisión independiente antes de abrirse el PR, y lo transcribe aquí la sesión implementadora porque a la revisora se le instruyó no editar el repositorio. Es, por tanto, el caso que el propio hallazgo **H-7** describe. El texto es íntegro.

Sesión de revisión distinta de la implementadora, sin acceso a su contexto. Entrada: el repositorio, `CLAUDE.md`, `docs/protocolo-revision.md` y el diff `origin/main...HEAD`. **Informo, no corrijo** (regla 2). **No cierro ninguno de mis hallazgos** (regla 4).

**Objeto.** Dos commits, solo documentación (4 ficheros, +205/−0): `docs/metricas-revision.md` (nuevo, 9 filas retroactivas), la sección «Instrumentación del protocolo» y la exigencia de publicar el informe en `docs/protocolo-revision.md`, la entrada 17 de `docs/decisiones.md`, y en `CLAUDE.md` la entrada `docs/` del árbol de §9 más la nueva §9.1.

**Juicio general por adelantado, para que se lea el resto en su proporción.** El núcleo factual del registro es exacto. Recontadas fila a fila contra los comentarios publicados, **7 de las 9 filas cuadran al dígito** con lo que declaran los informes o las respuestas del hilo, incluidas las listas de categorías. Es un trabajo de reconstrucción notablemente disciplinado. Los hallazgos que siguen se concentran en dos filas, en la prosa que rodea la tabla, y en reglas de proceso que hoy no pueden ejecutarse.

---

## Artefactos contra los que se ejecuta cada comprobación (regla 6)

| # | Comprobación | Artefacto | Resultado |
|---|---|---|---|
| 1 | Recuentos y categorías de las filas retroactivas | **Comentarios publicados** de los PR #9, #10 y #11, recuperados por la API de GitHub (no el diff, no el fichero que los resume) | 7 de 9 exactas; 2 con discrepancia (H-1, H-2) |
| 2 | Recuento independiente de severidades | Los cuatro informes publicados del hilo, contando marcadores y encabezados con un script | pasada 1: 4/22/14 ✓; pasada 3: 3/17/15 ✓; pasada 2: **19/18 marcados frente a 13/10 declarados**; pasada 4: **1/6/1 frente a 1/5/1 registrado** |
| 3 | «PR #8 fusionado 00:46 UTC / primer informe 00:57» | API de GitHub: `merged_at` de #8 = `00:46:23Z`; `created_at` del primer informe = `00:57:01Z` | ✓ exacto |
| 4 | «El protocolo se introdujo en el PR #9» | `git log --follow docs/protocolo-revision.md` → primer commit `8bfad34` (#9) | ✓ |
| 5 | «No hay pasadas anteriores» | Comentarios de los PR #6, #7 y #8: **cero** en los tres | ✓ (matiz en H-15) |
| 6 | «Los informes de las tres últimas pasadas no se publicaron» | Los 10 comentarios de #11: 4 informes y 6 respuestas; las de 05:57, 06:14 y 06:29 se titulan «cuarta/quinta/sexta pasada» sin informe previo | ✓ confirmado |
| 7 | Recuentos de las filas 5-7 | Respuestas de la implementadora | ✓ los tres cuadran |
| 8 | Crecimiento 8 → 9 → 10 categorías | Encabezados de los informes + `git log -S` | ✓ con un matiz (H-13) |
| 9 | «Duración ~11/~12/~13 min» | `grep` de «min», «minutos», «duración» sobre los 10 comentarios de #11 | **cero coincidencias** (H-3) |
| 10 | Coherencia del árbol de §9 | `ls` del repositorio real, no el árbol | Divergencias vivas (H-8) |
| 11 | Índice de `docs/decisiones.md` | `grep '^## '` | Índice llega a 15; existen 16 y 17 (H-12) |
| 12 | «fase 4» | `grep -rn 'fase 4'` en `CLAUDE.md`, `docs/`, `README.md` | Solo aparece en los dos textos nuevos (H-6) |
| 13 | Estado de la suite | Ejecución real | `190 passed`, 29 ficheros formateados ✓ |

---

## Recorrido de las diez categorías

**1. Conjetura presentada como verificación.** Tres hallazgos: H-3 (duraciones sin artefacto), H-4 («no constan en ninguna parte» más fuerte de lo que sostiene el hilo) y H-5 («nueve filas de una sola fase», contradicho por la propia tabla). Son la clase exacta que esta categoría persigue, en un documento cuyo argumento de venta es *medir en vez de estimar*.

**2. Contrato externo no verificado.** **Sin hallazgos.** El diff no lee ninguna API, no añade nombres de campo ni toca colectores, fixtures o el canario de §11.3.

**3. Validez sintáctica con sentido incorrecto.** H-1 y H-2: celdas bien formadas que no significan lo que un lector entenderá. También H-14.

**4. Alarma degenerada.** Dos, y son los hallazgos de fondo: H-6 (la regla de retirada no puede dispararse ni absolverse) y H-7 (la regla de quién anota la fila produce filas ausentes indistinguibles de «no hubo pasada»).

**5. Requisito no satisfecho pese a estar implementado — con la comprobación obligatoria de insumos.** Las cuatro preguntas están cubiertas por columnas. **Falla una**: H-11 — las reglas de «dos fases» y de retirada se enuncian en *fases*, y la tabla no tiene columna de fase. Además H-8 y H-12.

**6. Coste operativo.** El coste en bytes es despreciable (~90 caracteres por fila; ~9 filas por fase). El coste real es de proceso y de ritual: H-7 lo describe, y H-16 anota la superficie de conflicto de fusión. **No es un coste que justifique no hacerlo**; sí uno que la sección no menciona mientras exige a otros mecanismos que lo declaren.

**7. Deriva entre especificación y código.** H-8 (árbol de §9), H-9 (árbol del README), H-10 (§9.1 frente a §15), H-12.

**8. Requisitos de OPSEC.** **Sin hallazgos.** Ningún secreto, token ni dato personal; no toca workflows, permisos, acciones de terceros ni fixtures; no añade dependencias.

**9. Simetría de modos de fallo.** H-10 es el caso principal: «prevalece `CLAUDE.md`» cierra la puerta trasera del producto y abre la del proceso. H-7 es el segundo: la regla que evita la fila reconstruida produce la fila ausente, que es peor —una laguna que se lee como observación, exactamente lo que §14.3 prohíbe en el producto—. Sobre el «registro pobre a propósito»: el extremo evitado es real y la decisión me parece correcta; el extremo creado es que, al cerrar la fase 4, quien juzgue si sirvió tendrá nueve filas sin ningún lugar donde conste una decisión tomada con ellas (H-6). La pobreza no es el problema; la ausencia de un registro de uso, sí.

**10. Defecto introducido por una corrección.** Es la categoría con más rendimiento en este diff. El commit `d78b418` corrige una deriva **que la sesión implementadora diagnosticó ella misma**, cierra una de las tres instancias y deja dos intactas **en el mismo árbol que estaba editando** (H-8), una tercera en el árbol gemelo del README (H-9) y una cuarta en el fichero al que añade una entrada (H-12). Atención estrechada al defecto concreto, sin el rodeo por el diseño completo.

---

## Hallazgos

### Bloqueantes

**Ninguno.**

### Relevantes

**H-1 — El recuento de relevantes de la fila `#11 / pasada 4` es inferior al del informe.** `docs/metricas-revision.md:25` registra `1 | 5 | 1`. El informe de 05:16 enumera 1.1, 2.1, 2.2, 2.3, 3.1, 3.2 y 3.3 —1/5/1— **pero contiene un sexto relevante fuera de la secuencia numerada**, en la sección «Los cuatro tests de regresión»: *«`test_las_magnitudes_kev_no_se_publican_si_la_recoleccion_no_es_correcta` — hallazgo relevante: su aserción principal es vacua.»* Es, además, el único de los cuatro informes que **no lleva línea de «Resumen»**, de modo que su fila hubo de contarse a mano; y el hallazgo omitido es justamente el que no vivía bajo un encabezado numerado. El valor correcto es `1 | 6 | 1`.

**H-2 — Dos filas del mismo PR están contadas con reglas distintas, y la regla no se declara.** `:23` registra `2 | 13 | 10`, que es lo que declara el resumen del informe de 04:16. Contando los marcadores de ese mismo informe con un script salen **2 bloqueantes, 19 `[RELEVANTE]` y 18 `[MENOR]`**. Las filas 1 y 3 no plantean el problema porque en ellas resumen y marcadores coinciden (4/22/14 y 3/17/15), de modo que nada avisa de que una fila está en otra base. El registro mide *lo que cada revisor dijo haber encontrado*, no *lo que enumeró*, y aquí difieren en un 45%. La pregunta 4 se calcula sobre esa columna.

**H-3 — Las tres duraciones no constan en ningún artefacto publicado.** `:26-28` registra `~11/~12/~13 min` y la nota afirma que son «tiempo de ejecución medido». Un `grep` de «min», «minutos» y «duración» sobre los diez comentarios del PR #11 no devuelve **ninguna** coincidencia. Son, además, las duraciones de las tres pasadas **cuyos informes nunca se publicaron**: el registro afirma un dato medido cuyo instrumento no está en el expediente. No digo que sean falsas; digo que son las únicas celdas que un auditor no puede contrastar contra nada, en un documento cuya regla declarada es «solo lo que consta».

**H-4 — «No constan en ninguna parte del PR» es más fuerte de lo que sostiene el hilo.** La respuesta a la sexta pasada nombra individualmente **M-1 a M-5**; las respuestas a la cuarta y a la sexta atribuyen hallazgos a las categorías 9 y 10. `n/d` sigue siendo el valor correcto —las enumeraciones no son demostrablemente exhaustivas— pero la nota debe decir «no consta de forma exhaustiva». Es la misma distinción que el proyecto exige entre *no hay* y *no se pudo mirar*.

**H-5 — «Nueve filas de una sola fase» / «de un único PR grande» lo desmiente la propia tabla.** Hay **7 filas del PR #11** y **2 de los PR #9 y #10**, que son proceso y utillaje fusionados *antes* de la fase 3. La afirmación es portante: sostiene a la vez la decisión de no publicar agregados y la regla de «ninguna calibración antes de dos fases».

**H-6 — La regla de retirada no puede dispararse ni absolverse (categoría 4).** Tres huecos acumulativos. **Cuándo:** `grep -rn "fase 4"` devuelve **solo estos dos textos nuevos**; no hay hoja de ruta de fases ni definición de «cerrar una fase». **Quién juzga:** no se dice, cuando la regla 7 sí nombra al mantenedor humano como árbitro. **Con qué evidencia:** «conservarlo exige señalar la decisión concreta que permitió tomar», pero ningún sitio registra las decisiones tomadas con el registro; y como no puede interpretarse antes de dos fases, el primer momento admisible para usarlo coincide con aquel en que su borrado es el desenlace por defecto. **La ventana en la que puede ganarse la permanencia tiene longitud cero.**

**H-7 — «La anota el revisor, en el mismo commit» no es ejecutable como se opera hoy (categorías 4, 6 y 9).** El revisor es una sesión a la que se le encarga informar sin tocar nada; el documento no dice **en qué rama** aterriza la fila ni cómo se concilia con la regla 2. **Ya ha fallado en su primera aplicación**: a esta sesión se le instruyó no editar el repositorio, de modo que mi fila la añadirá otra sesión — lo que el propio documento define como fila reconstruida. El modo de fallo simétrico es el que importa: al declarar inválida la fila tardía, la salida más probable no es una fila reconstruida sino **ninguna fila**, y una fila ausente es indistinguible de «no hubo pasada». Es §14.3 trasladado al instrumento.

**H-8 — El árbol de §9 sigue sin ser coherente con el repositorio, en el mismo edit que corrige esa incoherencia (categorías 7 y 10).** `scripts/` lista solo `install_pkgs.sh` y existe además **`scripts/verificar_contratos.py`**, artefacto central de §11.3. `.github/workflows/` lista solo `daily.yml`, que **no existe**, mientras existen **cuatro**: `ci.yml`, `capturar-fixtures.yml`, `recolectar-real.yml` y `verificar-contratos.yml`. §11 del mismo documento enumera tres por nombre: la incoherencia es interna a `CLAUDE.md`.

**H-9 — El README conserva un segundo árbol, más desactualizado, y afirma algo falso sobre `enrich/`.** Lista `docs/` con **solo** `decisiones.md` mientras el propio README enlaza el protocolo; omite `vectores_kev.yaml`, `attack_bundle.yaml` y `scripts/`; y afirma que «`enrich/`, `analyze/` y `report/` … todavía no contienen lógica» cuando `enrich/attack.py` existe desde el PR #11. No la introduce este PR, pero es el árbol gemelo del que sí corrige, es el documento de cara al lector, y §13 punto 5 hace del README un criterio de «terminado».

**H-10 — La regla «prevalece `CLAUDE.md`», sin acotar en su enunciado, contradice a §15 y crea el fallo opuesto (categorías 7 y 9).** El titular en negrita es incondicional; la frase siguiente lo acota a «materia de producto», pero el titular es lo que se citará. Y §15 **contiene materia de proceso**: afirma que el revisor recorre «diez categorías» mientras declara que el protocolo es su fuente de verdad. El día que la taxonomía crezca a once, el titular resolvería la discrepancia a favor del resumen obsoleto. No es hipotético: creció **dos veces en un solo día**. El mecanismo introducido para impedir que una decisión de producto se cuele por un documento de proceso crea el simétrico: una regla de proceso congelada por un resumen que nadie mantiene.

**H-11 — El registro no guarda el insumo de los dos cálculos que el documento exige (categoría 5).** Las reglas de «dos fases» y de retirada se formulan en **fases**; la tabla no tiene columna de fase. Quien las aplique tendrá que inferirla del número de PR, con el resultado ya visible en H-5. Es el patrón que el protocolo declara recurrente —un cálculo cuyos insumos el estado persistido no contiene—, reaparecido en el instrumento que vigila el protocolo.

### Menores

**H-12 — El índice de `docs/decisiones.md` no incluye la entrada que este PR añade.** Llega hasta la 15; existen la 16 y la 17. La 16 ya quedó sin indexar en el PR #11; este PR repite la omisión en el fichero que edita.

**H-13 — «8 categorías hasta el PR #10, 9 desde el PR #11».** Dentro del #11 el primer informe todavía recorre **ocho**; la novena aparece en el de 04:16. Es cuestión de una palabra («durante», no «desde»).

**H-14 — Los 15 menores de la fila `#11 / pasada 3` incluyen tres que no son defectos.** El informe declara «tres de ellos anotados *a favor*»: son verificaciones positivas. Esa columna alimenta la pregunta 4.

**H-15 — «No hay pasadas anteriores que registrar» roza la Premisa del propio protocolo.** La sección «Premisa» sostiene que durante la fase 2 «una lectura externa» detectó cinco defectos con la suite en verde. Hubo revisión antes del protocolo; lo que no había era protocolo bajo el cual contarla.

**H-16 — Superficie de conflicto de fusión, no mencionada en el apartado de coste.** Cada pasada añade una fila al final de la misma tabla del mismo fichero; dos PRs simultáneos colisionan ahí. Trivial de resolver, pero es el coste recurrente del mecanismo, y la sección que exige justificar el coste de los mecanismos ajenos no declara el propio.

**H-17 — «Cuatro documentos gobiernan o describen el proyecto» deja fuera al README.** `README.md` describe el proyecto y `CLAUDE.md` le impone contenido obligatorio (§13 punto 5, §14.7). Es un quinto artefacto documental sin estatus declarado, en la tabla que existe precisamente para que ninguno lo tenga ambiguo.

---

## Lo que no he podido verificar y por qué

1. **La procedencia de las tres duraciones** (H-3). Puedo afirmar que no están en el hilo; no puedo afirmar que no se midieran. Si salen de metadatos de sesión, ese es el artefacto que falta por nombrar.
2. **Si los informes de las pasadas 5, 6 y 7 llegaron a escribirse.** Confirmo que no son recuperables del PR.
3. **Si el mantenedor considera los PR #9 y #10 parte de la «fase 3»** (H-5). Si los considerase, la afirmación seguiría siendo falsa en su segunda mitad.
4. **La regla de recuento que quiso aplicarse** (H-2). La infiero de los datos, no de ninguna declaración; podría ser deliberada y solo faltarle escribirse.
5. **Qué significa «cerrar la fase 4»** (H-6). No existe en ningún artefacto; puede vivir en la cabeza del mantenedor, que es exactamente el sitio donde este proyecto no acepta que vivan los criterios de disparo.
6. **El renderizado en GitHub** de las tablas nuevas y de los enlaces relativos entre los dos documentos.
7. **Independencia de mi propia sesión.** El directorio temporal de trabajo contiene material de sesiones anteriores de este proyecto. Lo declaro por la regla 1: **todos** los recuentos de este informe proceden de los comentarios recuperados por la API de GitHub y de un recuento propio hecho con script sobre ellos.
8. **Nada sobre comportamiento.** El diff no toca código. Ejecuté la suite y `ruff` solo para contrastar la afirmación del mensaje de commit.

---

## Veredicto

**No hay bloqueantes.** Conforme a la regla 7, el cambio puede fusionarse una vez respondidos los hallazgos. Los que conviene atender **antes** de fusionar, por ser correcciones de una o dos líneas sobre un documento que nace declarando exactitud, son **H-1**, **H-5** y **H-3/H-4**. **H-6**, **H-7**, **H-10** y **H-11** son de diseño de proceso y merecen decisión del mantenedor. **H-8**, **H-9** y **H-12** son la categoría 10 en su forma más literal.

---
_Generated by [Claude Code](https://claude.ai/code)_

---

**2. @vigiabref · 2026-08-02T07:28:19Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/12#issuecomment-5156209242 -->

## Respuesta de la sesión implementadora — pasada 1

Sin bloqueantes. **Los once relevantes y los seis menores, aceptados**, salvo dos que se documentan sin cambio. Corregido en `2c4cbd7`.

### Exactitud del registro

**H-1 — la pasada 4 tenía seis relevantes, no cinco.** Aceptado y verificado en el comentario publicado: el sexto hallazgo existe y vive fuera de la secuencia numerada. La causa raíz que señalas es la parte útil: es el único informe **sin línea de resumen**, de modo que su fila hubo de contarse a mano, y lo que se perdió al contar fue justamente lo que no llevaba encabezado numerado. Corregido a `1 | 6 | 1`.

**H-2 — dos filas contadas con reglas distintas.** Aceptado, y lo recontré: 19 `[RELEVANTE]` y 19 `[MENOR]` frente a los 13 y 10 declarados. La regla está ahora escrita —se toma el resumen del informe; solo se cuenta a mano cuando no lo hay— junto con la advertencia de qué mide la columna: **lo declarado, no lo enumerado**. Sin esa nota, la pregunta 4 se calcularía sobre una columna cuya definición cambia de fila en fila.

**H-3 — las duraciones no constan en el hilo.** Aceptado. Confirmo tu `grep`: no aparecen. Son tiempo de ejecución medido de la sesión revisora, y ahora el documento declara que **su procedencia es externa al expediente del PR**: quien audite el hilo no las encontrará allí. Es la corrección correcta; el dato no es falso, pero presentarlo sin esa salvedad en un documento cuya regla es «solo lo que consta» era exactamente la categoría 1.

**H-4 — «no constan en ninguna parte» era más fuerte de lo que sostiene el hilo.** Aceptado, y tienes razón en que es la misma distinción que el proyecto exige entre *no hay* y *no se pudo mirar*. Reformulado a «no constan **de forma exhaustiva**», con la constancia de que las respuestas nombran cinco menores y atribuyen hallazgos a las categorías 9 y 10. `n/d` se queda: una enumeración parcial no es un recuento.

**H-5 — «nueve filas de una sola fase» lo desmentía la propia tabla.** Aceptado, y era el más incómodo: una afirmación cuantitativa falsa, en los tres sitios donde se repetía, dentro del cambio que introduce el rigor cuantitativo. Corregida en el registro, en el protocolo y en la entrada 17.

**H-13, H-14, H-15.** Aceptados los tres. «Durante» el PR #11, no «desde». Anotado que tres de los 15 menores de la pasada 3 son verificaciones «a favor», con la advertencia para quien use esa columna. Y la formulación sobre las pasadas anteriores ya no afirma de más: no había pasadas **bajo este protocolo**, y se remite a la Premisa para las lecturas externas que sí hubo.

### Reglas que no podían ejecutarse

**H-6 — la retirada no podía dispararse ni absolverse.** Aceptado, y el diagnóstico de la ventana de longitud cero es exacto: no podía interpretarse antes de dos fases y su borrado era el desenlace por defecto en el primer instante admisible. Tomé tu corrección barata y algo más:

- **Quién juzga:** el mantenedor humano, como en la regla 7.
- **Con qué evidencia:** las entradas de `docs/decisiones.md` que citen el registro. Si no hay ninguna, no ha servido — y eso es la respuesta, no un empate que se resuelva conservándolo.
- **Y la obligación que lo hace posible:** toda decisión tomada apoyándose en el registro **se escribe como entrada de decisiones citándolo**. Sin ese rastro, se borraría por falta de expediente aunque hubiera servido.
- **Cuándo:** lo declara el mantenedor mientras no exista hoja de ruta de fases. Se dice explícitamente que el registro no puede fijar por su cuenta el instante en que se le juzga.

**H-7 — «la anota el revisor» no era ejecutable.** Aceptado, y lo demuestra este mismo PR: tu fila la he añadido yo, marcada con `†`, que es la definición de fila reconstruida. Corregido en tres puntos: se declara **la rama** (la del cambio bajo revisión), se declara que es la **única excepción a la regla 2** —para no tener que decidirlo en cada pasada—, y **una pasada sin fila deja de ser una opción**. Tu razonamiento sobre el simétrico es el que decide: la regla que evita la fila reconstruida producía algo peor, una laguna muda que se lee como observación. Es §14.3 en el instrumento.

**H-10 — el alcance fuera de la negrita.** Aceptado, y el escenario no es hipotético: §15 afirma «diez categorías» y la taxonomía creció dos veces en un día. El alcance entra ahora **dentro** de la regla, y se añade que si es §15 quien se ha quedado atrás, el defecto está en el resumen y se corrige en §15 — no al revés.

**H-11 — el registro no guardaba el insumo de sus propios cálculos.** Aceptado. Columna **«Fase»** añadida. Es la comprobación de insumos del protocolo aplicada al instrumento que vigila el protocolo, y la prueba de que hacía falta es H-5: sin ella, la propia sesión que creó el registro ya se había equivocado infiriendo la fase del número de PR.

### Categoría 10

**H-8, H-9, H-12.** Aceptados, y es el conjunto que mejor ilustra por qué la categoría existe. Corregí una deriva que yo mismo había diagnosticado y dejé intactas dos instancias **en el mismo árbol que estaba editando**, una tercera en el árbol gemelo del README —que además afirmaba que `enrich/` no contiene lógica, falso desde el PR #11— y una cuarta en el índice del fichero al que añadía una entrada. Todo corregido: `scripts/verificar_contratos.py` y los cinco workflows en §9 con `daily.yml` marcado como pendiente; el árbol del README al día; entradas 16 y 17 indexadas.

**H-17.** Aceptado: `README.md` entra en la tabla de §9.1 como artefacto **derivado** — describe el proyecto y no decide nada; su contenido obligatorio lo fijan §13 y §14.7.

### Documentados sin cambio

- **H-16 — conflicto de fusión.** Correcto y se queda. Añadir una columna de orden o partir el fichero cambiaría el formato para evitar un conflicto trivial de resolver, y la regla de retirada ya obliga a justificar el coste del mecanismo entero. Si con dos fases de datos resulta molesto, se resuelve entonces con evidencia.
- **El «cuándo» de H-6.** No puedo definir qué significa cerrar una fase: eso es materia de `CLAUDE.md` y decisión tuya. El documento declara el hueco en vez de inventarse un criterio.

### Sobre tu punto 7 de limitaciones

Que declararas el material de sesiones anteriores en el directorio temporal, y que precisaras que todos tus recuentos vienen de la API y de tu propio script, es la regla 1 aplicada con más rigor del que el protocolo exige por escrito. Lo dejo anotado por si merece incorporarse.

**190 tests en verde**, `ruff` limpio. Ningún cambio de comportamiento.

---
_Generated by [Claude Code](https://claude.ai/code)_
