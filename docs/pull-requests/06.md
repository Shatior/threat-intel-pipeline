# Pull request #6 — Correcciones pre-fase 3: esquema, descartes, estado dividido y gzip

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-01T19:07:06Z |
| **Fusionado** | 2026-08-01T22:56:41Z |
| **Commit de fusión** | `ea8a990740d809b3fafa6bff5dd697f8176dd596` |
| **Rama** | `claude/correcciones-pre-fase3` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/6 |
| **Comentarios archivados** | 0 |

## Descripción

Correcciones previas a la fase 3 (enriquecimiento MITRE ATT&CK, §5). Sin funcionalidad nueva: consolidan el esquema, el resultado de recolección y la persistencia del estado.

## Cambios

**1. `file-sha1` en el esquema (§4) y en ThreatFox**
- Añadido `file-sha1` a `TipoIndicador` y a las excepciones de idioma de §10.
- Mapeo de `sha1_hash` → `file-sha1` en el colector de ThreatFox. En la recolección real recupera 361 IOCs que antes se descartaban como tipo no soportado.

**2. Separación de descartes en el resultado de recolección (§14.3, §14.4)**
- `descartados_invalidos` (incumplen el esquema §4) → elevan la fuente a `parcial`.
- `no_soportados` (tipo sin equivalencia en §4) → se cuentan y se declaran, pero **no** degradan el estado: es una limitación del esquema, no un fallo de la fuente.

**3. Estado dividido en dos (§9)**
- `data/state/` (versionado): estado mínimo para el diferencial de §6.
- `data/cache/` (no versionado): volcado completo con `raw`.

**4. Formato de `ventana_consultada` (§14.3)**
- Corregido a `P<n>D/<instante>`: la duración va delante del instante final porque la ventana mira hacia atrás.

**5. Estado mínimo: `type`, `value` y gzip (§6, §9)**
- Añadidos `type` y `value` al estado mínimo. Sin ellos el cálculo de indicadores **caídos** de §6 es imposible: la `clave_canonica` es un `sha256` no invertible y el volcado con valores no se versiona, así que no habría forma de nombrar el indicador ausente respecto a la ejecución anterior.
- El estado mínimo se guarda **comprimido con gzip y sin indentación** (`indicadores.json.gz`), con `mtime=0` para que sea determinista: se sacrifica la legibilidad del diff a cambio de un historial de git sostenible; un estado idéntico produce bytes idénticos, sin diffs espurios.

## Tests

81 tests en verde, `ruff format --check` y `ruff check` limpios. Nuevo `tests/test_persistencia.py` que cubre el volcado gzip (determinismo, compacidad y presencia de `type`/`value`).

## Nota sobre la cobertura de `last_seen` en ThreatFox

Diagnóstico solicitado, sin corrección en este PR: `last_seen` viene nulo en ~76% de los registros de ThreatFox y la cobertura reporta `{}`. La causa es que **`last_seen` no está declarado en `CAMPOS_ESPERADOS`** (se excluye deliberadamente, junto a `reference` y `tags`, por faltar legítimamente a menudo). No es que su cobertura supere el umbral: es que el chequeo de §14.4 nunca lo evalúa. Pendiente de decidir si debe vigilarse.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
