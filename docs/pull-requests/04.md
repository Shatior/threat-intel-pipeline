# Pull request #4 — Fase 2 — Colectores: política HTTP común, CISA KEV y ThreatFox (§14)

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-01T17:54:04Z |
| **Fusionado** | 2026-08-01T18:02:26Z |
| **Commit de fusión** | `a8efea2c76b4ad44699f0e3570b22d0a422e8931` |
| **Rama** | `claude/fase2-colectores` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/4 |
| **Comentarios archivados** | 0 |

## Descripción

## Contexto

Segunda fase del proyecto: la **recolección** de las dos fuentes de §3, conforme a la nueva sección **§14** de `CLAUDE.md`. No incluye enriquecimiento ATT&CK (§5), diferencial (§6) ni informe (§8) — cada uno llegará en su fase.

## Qué incluye

- **Política HTTP común (§14.2)** en `collect/base.py` (`ClienteHTTP`), heredada por todos los colectores: timeout siempre presente, ≤3 reintentos con retroceso exponencial (base 2) y jitter que solo suma, `Retry-After` en segundos y en fecha HTTP con techo de espera, sin reintento ante 4xx≠408/429, y **tope de peticiones por ejecución**. Transporte/espera/jitter/reloj inyectables → tests sin red.
- **Colector CISA KEV** (`collect/cisa_kev.py`): peticiones condicionales (`ETag`/`If-None-Match`; 304 = correcta sin cambios); confianza alta fija (§7).
- **Colector ThreatFox** (`collect/threatfox.py`): `Auth-Key` desde `ABUSECH_AUTH_KEY` (fail-fast si falta), ventana de 5 días, mapeo `ioc_type`→§4 con descarte contado de tipos no soportados.
- **Resultado de recolección con estados** `correcta`/`parcial`/`fallida` (§14.3) y su persistencia en `data/state/` (`persistencia.py`); subcomando **`recolectar`** en el CLI (código ≠ 0 ante fallo total, sin generar informe, §14.6).
- **Validación en frontera (§14.4)**: registros que no validan se cuentan y elevan a `parcial`, y **cobertura de campos esperados** (enmienda 5): si un campo habitual cae por debajo del umbral (80%), la fuente pasa a `parcial` declarando qué campo y en qué porcentaje.

## Enmiendas a §14 aplicadas

1. Ventana de ThreatFox a **5 días** (holgura sobre la penalización de 72 h de abuse.ch).
2. Verificación del **estado de aplicación** (`query_status`) antes de dar la recolección por correcta; sin reintento ante limitación por tasa.
3. **Tope de peticiones** por ejecución como red de seguridad.
4. Sección **§14.7 de evaluación de fuentes** (reflejada en el README).
5. **Cobertura de campos esperados** en la validación en frontera.

## Fixtures reales, sin red

`tests/fixtures/` contiene respuestas **reales** de ambas fuentes, capturadas por el workflow `capturar-fixtures` y reducidas: ThreatFox con variedad de tipos (dominio, url, sha256, md5, ip:port) más un registro malformado (§14.4); CISA KEV con 3 entradas del catálogo. `tests/fixtures/README.md` documenta el origen exacto de cada registro. Sin claves ni datos personales. La captura confirmó que ThreatFox usa `first_seen`/`last_seen` (no `*_utc`): los nombres del colector eran correctos.

## Verificación

- `pytest` → **74 passed**, todos con fixtures y sin acceso a red (§14.5).
- `ruff check` y `ruff format --check` limpios.
- `python -m threatintel recolectar` declara estados y vuelca a `data/state/` sin generar informe.

## Fuera de alcance

Enriquecimiento (§5), diferencial (§6), informe (§8) y workflow diario (§11.2).

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
