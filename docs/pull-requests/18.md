# Pull request #18 — Bloque 3 de la fase 4: el diferencial de §6, y la reparación del encargo del revisor

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T20:44:07Z |
| **Fusionado** | 2026-08-02T20:52:27Z |
| **Commit de fusión** | `7a71a3d114b2547f0ba865fdc757c7f0ee66a6b2` |
| **Rama** | `claude/fase4-diferencial` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/18 |
| **Comentarios archivados** | 0 |

## Descripción

## Qué entra

Dos cosas, y la segunda ocurrió por causa de la primera.

### 1. El diferencial de §6 (bloque 3 de la fase 4)

- **Estado mínimo formato 2 (§9).** El fichero versionado deja de ser una lista y pasa al objeto especificado: `formato`, `marcas_de_agua` **por fuente**, `linea_base_vigente` e indicadores con su mapa `fuentes` y un bloque `kev` en los de tipo `vulnerability`. `momento_ejecucion` **no** se persiste: sus dos usos consumen el valor en curso.
- **Modo del informe en dos instantes (§6.2).** Candidato antes de tocar la red —línea base con los seis motivos exhaustivos, o diferencial—; final tras recolectar, donde el fallo total prevalece.
- **Tres conjuntos por fuente (§6.1, §6.4)** con techo de validez tomado de la `ventana_consultada` que declara la propia recolección, regla positiva de la marca de agua, supresión de caídos ante cero indicadores, arrastre intacto de la fuente que no alcanza `correcta`, fuente sin marca previa «en línea base», y poda de caídas a 30 días.
- **Determinismo**, comprobado en los dos planos que se rompen por separado: el orden de los conjuntos y los bytes del fichero versionado, frente al orden de los registros **y** al de las fuentes.
- **`config/settings.yaml`** gana los tres parámetros del diferencial, con el código que los lee y declarados como valores iniciales, no como cifras medidas.

**337 tests en verde**, `ruff` limpio, `verificar_contratos.py --sin-red` en 0.

### 2. Reparación del encargo del revisor

La primera pasada de revisión de este bloque consumió **1 h 55 min y no produjo ningún artefacto**. Un protocolo cuya aplicación no termina no está congelado: está roto, y esa es la excepción que el propio congelamiento prevé.

El diagnóstico, medido: **el coste de una pasada escala con el corpus del proyecto, no con el diff.** El encargo mandaba leer la especificación entera (2.621 líneas), el protocolo (570), los pendientes (429) y el histórico de actas (**20 ficheros, 956 KB**), con un mandato de mutación sin cota y una entrega todo-o-nada.

Se acota en seis puntos (`docs/protocolo-revision.md`, sección «Reparación del congelamiento»): corpus limitado al diff y a las secciones de `CLAUDE.md` que toca; presupuesto de 10 minutos y 30 mutaciones; acta incremental; una sola pasada por bloque; cobertura parcial **declarada**; y orden de prioridad por consecuencia en producción. No cambian la taxonomía, las severidades ni el recuento: el defecto no estaba en qué se busca, sino en cuánto hay que leer y hasta cuándo hay que buscar.

## Revisión independiente

Acta en `docs/revisiones/claude-fase4-diferencial--pasada-1.md`, commiteada sin modificar, `sha256` `f35b0b9e14a12ebe40b88c529036daf233dee7a31bd58f288a3a8fdc99395546`.

**Primera pasada bajo el encargo acotado: 6 min de los 10 presupuestados, 20 mutaciones (19 muertas), 1 bloqueante, 1 relevante y 1 menor.** Categorías recorridas por prioridad 3-4-5-9, luego 1-2-10, luego 8; **declara sin recorrer** las 6, 7 y 11, y la 2 parcialmente.

### Bloqueante — corregido

El validador condicional de CISA KEV se leía de forma incondicional, pese a que §14.2 exige descartarlo cuando el estado mínimo se perdió o no se interpreta — requisito que la propia especificación **difería expresamente a este bloque** («pendiente de implementación: la carga del estado mínimo llega con el diferencial de §6») y que §14.5 enumera. El insumo llegó en este bloque y no se cableó.

La consecuencia: un estado perdido más un 304 escribe marca de agua con cero entradas KEV, y todas las ejecuciones siguientes declaran «sin cambios» sobre un catálogo que el pipeline nunca leyó.

La corrección va en dos planos porque una mutación demostró que corregir solo el colector dejaba el cableado sin cubrir: el colector recibe `usar_validadores` y el CLI se lo transporta desde `carga.estado is not None`, que es exactamente la condición de §14.2. Ambos planos tienen prueba y ambas mutaciones mueren.

### Relevante — corregido

La variación por familia devolvía el diccionario vacío tanto cuando **no era calculable** como cuando **no había variación**, haciendo indistinguible «no se calculó» de «dio cero» — literalmente lo que §8.3 obliga a declarar por separado. Ahora devuelve `None` en el primer caso y el CLI declara los tres desenlaces. Se cubre además el filtro de fuentes publicables, cuya mutación había sobrevivido.

### Menor — corregido

Un estado que declara un formato posterior al conocido se rechaza como no interpretable en vez de leerse como el actual: un formato futuro puede cambiar la semántica sin cambiar la forma.

## Lo que el acta declara no verificado

La cadena de cuatro pasos del bloqueante de extremo a extremo (confirmada por lectura y por ausencia de test, no por ejecución); las declaraciones de §8.3, que hoy solo existen como líneas de log porque el renderizado del informe llega en el bloque 4; y la coherencia de `_identidad_de_familia` con el recuento de §8.1 sobre un caso real con `raw`.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
