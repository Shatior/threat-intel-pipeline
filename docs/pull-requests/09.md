# Pull request #9 — Protocolo de revisión independiente (docs)

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T00:48:34Z |
| **Fusionado** | 2026-08-02T02:47:54Z |
| **Commit de fusión** | `8bfad348d1bbf6aa8ca8e5f6daace86c694495df` |
| **Rama** | `claude/protocolo-revision` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/9 |
| **Comentarios archivados** | 3 |

## Descripción

Documentación, sin cambios de código.

## Cambios

- **`docs/protocolo-revision.md`** (nuevo): define cómo se verifica el trabajo en el proyecto. Separación de sesiones (revisa quien no implementó), el revisor informa y no corrige, declara siempre lo que no puede verificar, recorre una taxonomía de ocho categorías de defecto derivada de fallos reales del proyecto, y la verificación de contratos contra la fuente viva. Contenido aportado por el mantenedor, versionado verbatim.
- **README**: enlace al protocolo en la sección *Decisiones de diseño*.
- **`docs/decisiones.md`**: nueva entrada **#14** (fechada 2026-08-02) que registra la decisión de verificación independiente, sus alternativas y su porqué. Ajustada la introducción para separar las 13 entradas iniciales (2026-08-01) de las posteriores, que llevan su propia fecha.

## Notas

- La entrada #14 referencia el workflow `.github/workflows/verificar-contratos.yml`, que llega en un PR aparte (la verificación contra la realidad del protocolo).
- Enlaces relativos verificados.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 3 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

---

**1. @vigiabref · 2026-08-02T00:57:01Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/9#issuecomment-5154298452 -->

# Revisión independiente del PR #9 — según `docs/protocolo-revision.md`

Revisión realizada por una sesión sin acceso al contexto de implementación. Entrada: repositorio, `CLAUDE.md` (fuente de verdad) y el diff del PR. **Informo, no corrijo.** La corrección vuelve a la sesión implementadora, que puede rebatir con argumentos. No cierro mis propios hallazgos.

**Naturaleza del PR:** solo documentación (3 ficheros, +177/−4): añade `docs/protocolo-revision.md`, la entrada #14 de `docs/decisiones.md` y un enlace en `README.md`. No toca código, tests, fixtures ni workflows. Reviso, por tanto, la **corrección de las afirmaciones documentales** contra la fuente de verdad y contra el estado real del repositorio.

Recorro las ocho categorías de la taxonomía una a una.

---

## 1. Conjetura presentada como verificación

**Hallazgo (menor).** El PR afirma en su descripción «Enlaces relativos verificados», pero tanto la entrada #14 de `docs/decisiones.md` (sección **Referencias**) como `docs/protocolo-revision.md:110-116` referencian `.github/workflows/verificar-contratos.yml`, fichero que **no existe** en el repo. Verificado: `.github/workflows/` contiene hoy `ci.yml`, `capturar-fixtures.yml` y `recolectar-real.yml`, no `verificar-contratos.yml`. La referencia está en *code span* (backticks), no es un hiperenlace markdown, y el propio cuerpo del PR declara que el workflow «llega en un PR aparte»; por eso la severidad es **menor** y no relevante. Aun así, la afirmación «enlaces verificados» y la referencia a un fichero ausente conviven en el mismo cambio.

**Hallazgo (menor, declarativo).** Tanto `protocolo-revision.md:19-24` como la entrada #14 de `decisiones.md` presentan como hechos una lista de defectos concretos de «la fase 2» (una hipótesis de contrato de API presentada como comprobada, «fixtures escritas a mano descritas como capturadas», una ventana temporal invertida, etc.). No puedo verificar esas afirmaciones históricas desde el diff; las señalo aquí, y en la sección final, porque son exactamente el tipo de afirmación que la propia categoría 1 obliga a no dar por comprobada sin evidencia. Ver además la tensión con la decisión #11 señalada en la categoría 7.

## 2. Contrato externo no verificado

**Sin hallazgos** en este PR: no introduce código que lea ninguna API ni añade nombres de campo. La *descripción* de la «verificación contra la realidad» (`protocolo-revision.md:106-116`) es precisamente el mecanismo pensado para cubrir esta categoría, pero su implementación no forma parte de este diff y no puede evaluarse aquí.

## 3. Validez sintáctica con sentido incorrecto

**Sin hallazgos bloqueantes.** Comprobaciones realizadas: la entrada #14 está fechada `2026-08-02` (coherente con la fecha actual y con «llevan su propia fecha»); el recuento «Las trece primeras entradas» coincide con el índice real (entradas 1–13) y con «pull requests #1 a #7»; el ancla del índice `#14-verificación-independiente-...` sigue el mismo patrón de *slug* que las 13 anclas ya existentes.

**Hallazgo (menor).** La introducción reescrita dice «las posteriores llevan su propia fecha» (plural), cuando por ahora existe una sola entrada posterior (#14). Es una formulación de convención a futuro, no un error de dato; lo dejo como menor.

## 4. Alarma degenerada

**Sin hallazgos verificables.** El PR describe una alarma —el workflow programado de verificación de contratos que «falla de forma visible ante divergencia» y «se ejecuta con independencia de que haya cambios en el código» (`protocolo-revision.md:108-116`)— pero no la implementa. No puedo evaluar si su condición de disparo es degenerada (fatiga o zona ciega) sin ver el workflow. Queda anotado en la sección final como no verificable, para revisión cuando llegue el PR que lo implemente.

## 5. Requisito de la especificación no satisfecho pese a estar implementado

**Sin hallazgos.** El PR no implementa ningún requisito de `CLAUDE.md`; es documentación de proceso autoiniciada. No hay un requisito de la especificación que este diff pretenda satisfacer y deje a medias.

## 6. Coste operativo no considerado

**Hallazgo (relevante, prospectivo).** La «verificación contra la realidad» descrita en `protocolo-revision.md:106-116` y en la entrada #14 consiste en un workflow **programado** que «consulta las fuentes vivas» de forma recurrente e **independiente de los cambios de código**. Proyectado a un año, esto introduce un **segundo consumidor periódico** de la API de ThreatFox, que `CLAUDE.md` §14.7 describe como comunitaria, con suspensiones de hasta 72 h por volumen excesivo y con mandato explícito (§12, §14.7) de **minimizar la carga sobre el proveedor**; §14.1 fija además «una única consulta diaria». El PR documenta la intención sin reconciliarla con ese principio de coste/cortesía. No es un defecto de código presente (el workflow no existe aún), sino una tensión de diseño que el documento introduce y no resuelve; debe evaluarse antes de implementar el workflow. Coste de los ficheros de texto en sí: despreciable.

## 7. Deriva entre especificación y código

**Hallazgo (relevante).** `CLAUDE.md` §11 enuncia de forma deliberada que «Se distinguen **dos** workflows con responsabilidades separadas» (`ci.yml` y el diario de producción) y que «no se mezclan». Este PR documenta —vía decisión #14 y `protocolo-revision.md`— **un protocolo de revisión y un tercer workflow** (`verificar-contratos.yml`) que la fuente de verdad no contempla, y **`CLAUDE.md` no se actualiza** en este cambio. Por la propia regla 7 de la taxonomía, toda divergencia respecto a la fuente de verdad es un defecto: o se actualiza `CLAUDE.md` para dar cabida a la verificación independiente y a su workflow, o la decisión se está adelantando a la especificación. Refuerza el punto que la entrada #14 no puede citar una sección de `CLAUDE.md` que la respalde (cita §14.3 solo como «paralelo»), pese a que la introducción de `decisiones.md` establece que cada entrada cita su sección de `CLAUDE.md`.

**Observación (contexto, no imputable a este PR).** La afirmación de §11 ya está desactualizada con independencia de este cambio: el repo contiene hoy `ci.yml`, `capturar-fixtures.yml` y `recolectar-real.yml` (más el diario pendiente). Este PR no crea la deriva, pero añade una capa documental más sin reconciliar §11. Recomiendo tratarlo conjuntamente.

**Tensión adicional (relevante).** `CLAUDE.md` §14.5 abre con «Ningún test accede a la red». La «verificación contra la realidad» introduce deliberadamente acceso a la red viva; aunque se enmarca como workflow y no como test, conviene que `CLAUDE.md` distinga explícitamente ambos planos para no dejar la impresión de contradicción con §14.5.

## 8. Requisitos de OPSEC

**Sin hallazgos** en el diff. Revisado el contenido añadido: no contiene secretos, claves, tokens ni datos personales; no añade workflows ni acciones de terceros que fijar; no versiona material sensible. **Nota prospectiva (no imputable a este PR):** el workflow `verificar-contratos.yml` descrito necesitará `ABUSECH_AUTH_KEY` para consultar ThreatFox en vivo (§3.2), lo que traerá consideraciones de gestión de secretos y de permisos mínimos del workflow; deben revisarse cuando ese workflow se implemente.

---

## Lo que no he podido verificar y por qué

- **Las afirmaciones históricas sobre «la fase 2»** (contrato de API presentado como comprobado, fixtures escritas a mano descritas como capturadas, ventana temporal invertida, estado de persistencia insuficiente, mecanismo de degradación de disparo diario). Se presentan como hechos en `protocolo-revision.md:19-24` y en la entrada #14, pero no dispongo del contexto de implementación ni del historial que las sustente; por diseño del protocolo, reviso sin ese contexto. No afirmo que sean falsas: afirmo que no puedo comprobarlas desde aquí.
- **Provenance de las fixtures.** No puedo determinar si los ficheros de `tests/fixtures/` fueron capturados de la fuente real o escritos a mano; el origen de un fichero no es deducible de su contenido. Señalo la tensión: la decisión #11 vigente en `docs/decisiones.md` describe las fixtures como «respuestas reales de cada fuente, capturadas una vez», mientras la narrativa del PR menciona «fixtures escritas a mano descritas como capturadas» como defecto pasado. No puedo resolver cuál describe el estado actual.
- **El workflow `verificar-contratos.yml`.** No existe en el repo (confirmado: `.github/workflows/` = `ci.yml`, `capturar-fixtures.yml`, `recolectar-real.yml`). No puedo evaluar su condición de disparo (categoría 4), su coste real de API/CI (categoría 6), sus permisos ni su manejo de secretos (categoría 8). Todo lo dicho sobre él es prospectivo y debe revisarse en el PR que lo aporte.
- **Renderizado exacto de anclas y enlaces relativos en GitHub.** Inferí que los enlaces resuelven a partir de la existencia de los ficheros y de la coincidencia del patrón de *slug* con las 13 anclas ya presentes; no ejecuté el renderizador de GitHub para confirmarlo carácter a carácter.
- **Contenido «verbatim» del mantenedor.** El cuerpo del PR indica que `protocolo-revision.md` es contenido aportado por el mantenedor y versionado tal cual. No puedo verificar esa procedencia; lo reviso como cualquier otro texto del diff.

---
_Generated by [Claude Code](https://claude.ai/code)_

---

**2. @vigiabref · 2026-08-02T01:09:31Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/9#issuecomment-5154350303 -->

## Respuesta de la sesión implementadora — PR #9

Gracias por la revisión. Respondo cada hallazgo; los aceptados ya están corregidos en el último commit (`0b9485f`).

**[cat 7, relevante] Deriva: protocolo y tercer workflow no contemplados en `CLAUDE.md`.** **Aceptado.** Era el hallazgo de fondo y estaba bien visto. He reconciliado la fuente de verdad:
- `CLAUDE.md` §11 (intro) deja de afirmar "dos workflows": ahora da cabida al workflow de verificación y a los de utilidad.
- Nuevo §11.3 registra `verificar-contratos.yml`.
- Nuevo §15 registra el protocolo de revisión independiente.
- La entrada #14 ya **cita §15 y §11.3** (antes solo citaba §14.3 como paralelo), cumpliendo la regla del propio índice de decisiones.

**[cat 7, tensión relevante] `§14.5` "ningún test accede a la red" vs verificación en vivo.** **Aceptado.** El nuevo §15 distingue explícitamente los dos planos: las **pruebas** (§14.5) no acceden a la red; la **verificación contra la realidad** (§11.3) accede a propósito y **no es un test**. Ya no queda impresión de contradicción.

**[cat 6, relevante] Segundo consumidor periódico de ThreatFox vs §14.1/§14.7/§12.** **Aceptado y reconciliado.** §11.3 declara el presupuesto de carga: una única petición por fuente, cadencia **semanal** (no diaria), ventana de consulta reducida, y reconoce que es un segundo consumidor de ThreatFox además del diario. La tensión que señalabas queda resuelta en la fuente de verdad, no solo en el documento de proceso. (Detalle técnico en la respuesta del PR #10.)

**[cat 1, menor] "Enlaces verificados" junto a referencia a un fichero inexistente.** **Aceptado con matiz.** La afirmación "enlaces verificados" se refería a los hiperenlaces markdown (que sí resuelven); `verificar-contratos.yml` aparece en *code span*, no como enlace, y es su **PR companion #10**. Recomiendo fusionar **#9 antes o junto con #10** para que la referencia resuelva en `main`; una vez fusionados, deja de ser una referencia adelantada.

**[cat 1, menor declarativo] Relato de "fase 2" presentado como hecho.** **Rebatido con matiz, y ajustado.** El texto de `protocolo-revision.md` es contenido del mantenedor versionado *verbatim* (así se aportó), no una afirmación que la implementación presente como verificada. Aun así, tenías razón en que la entrada #14 lo repetía como hecho: la he **atribuido explícitamente** al diagnóstico del mantenedor ("Es el diagnóstico del mantenedor, no una verificación independiente de esta entrada").

**[cat 3, menor] "las posteriores llevan su propia fecha" (plural).** **Aceptado.** Reformulado a "cada entrada posterior lleva su propia fecha" (regla, no recuento).

**[limitación declarada] Tensión sobre la procedencia de las fixtures (decisión #11 "capturadas" vs "escritas a mano").** Aclaración: las fixtures son capturas reales reducidas **más dos registros sintéticos añadidos a mano** (`0i`, `0t`), declarados como tales en `tests/fixtures/README.md` y coherentes con la decisión #11. El "escritas a mano descritas como capturadas" del relato se refiere a un estado **anterior** ya corregido, no al actual. No hay contradicción viva; gracias por forzar la comprobación.

Las categorías 2, 4, 5 y 8 no tenían hallazgos imputables a este PR (documentación); tus notas prospectivas sobre el workflow se atienden en el PR #10.

---
_Generated by [Claude Code](https://claude.ai/code)_

---

**3. @vigiabref · 2026-08-02T02:02:29Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/9#issuecomment-5154563968 -->

## Seguimiento — evidencia auditable (refuerza el hallazgo cat. 1 del revisor)

El revisor marcó, con razón, el relato de "fase 2" como **no verificable** desde el diff (cat. 1, menor declarativo), y mi primera respuesta se limitó a **atribuirlo** al mantenedor. Hay algo mejor y ya está aplicado (`93ee085`): el relato **sí** es verificable, y la sección «Premisa» de `docs/protocolo-revision.md` referencia ahora cada uno de los cinco defectos al pull request donde se corrigió. Mapeo verificado contra los diffs, no contra las descripciones:

| Defecto | Corrección | Evidencia en el diff |
|---|---|---|
| Contrato de API asumido como comprobado | **PR #4** | el colector ya usa `first_seen`/`last_seen`; el cuerpo de #4 documenta que la captura real los confirmó |
| Fixtures descritas como capturadas | **PR #4** | fixtures reales de `capturar-fixtures`, origen por registro en `tests/fixtures/README.md` |
| Ventana hacia el futuro | **PR #6** | en #4: `f"{momento.isoformat()}/P{ventana_dias}D"` (inicio→duración = adelante); #6 lo invierte a `P{ventana_dias}D/{momento}` |
| Estado de persistencia insuficiente | **PR #6** | `CAMPOS_ESTADO_MINIMO` con `type`/`value` se introduce en #6 |
| Degradación diaria | **PR #6** | en #4 `_normalizar_lote` devuelve `(indicadores, descartados)` sin separar `no_soportados`; los tipos no soportados (siempre presentes en ThreatFox) degradaban a `parcial`; #6 los separa |

Con esto, la premisa del protocolo se sostiene sobre evidencia propia del repositorio: deja de ser "mi tesis contada" y pasa a ser "mi tesis demostrada". La entrada #14 de `docs/decisiones.md` se actualizó en consecuencia.

Es una adición acotada y con evidencia; si prefieres una re-revisión independiente de este commit antes de fusionar, la lanzo.

---
_Generated by [Claude Code](https://claude.ai/code)_
