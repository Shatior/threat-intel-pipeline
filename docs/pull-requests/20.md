# Pull request #20 — Bloque 5 de la fase 4: el workflow diario de producción

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T22:42:29Z |
| **Fusionado** | 2026-08-02T22:43:15Z |
| **Commit de fusión** | `6c82288104197044fa475954166596a96f5ce38c` |
| **Rama** | `claude/fase4-daily` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/20 |
| **Comentarios archivados** | 0 |

## Descripción

## Qué entra

`.github/workflows/daily.yml`: programado a las 06:00 UTC y disparable a mano, ejecuta el ciclo completo, publica el informe de §8 y **commitea `reports/` y el estado mínimo versionado**. Es lo que hace alcanzable el punto 4 de §13 — hasta ahora el pipeline sabía producir el informe y el runner efímero lo tiraba.

Permisos mínimos (`permissions: {}` en la raíz, `contents: write` solo en el job), ambas acciones fijadas por SHA de commit, secreto por GitHub Secrets y enmascarado, y control de concurrencia sin cancelación —abandonar una ejecución a medias dejaría el estado escrito y el informe sin publicar—.

**424 tests en verde.** `tests/test_workflow_diario.py` es nuevo: un workflow no se ejecuta en la batería, pero sí es un artefacto con requisitos duros que se degradan en silencio.

### Una cuarta ruta versionada que ninguna sección enumera

Además de las tres de §9 y §14.3, se commitea **`data/state/validadores_http.json`**. Sin él el 304 **no ocurre nunca en producción**: el runner es efímero, de modo que «conservar el `ETag` en `data/state/`» (§14.2) solo significa algo si el fichero se versiona. §5.2 estaría describiendo como «caso habitual» un camino que el pipeline no recorre jamás.

## Revisión independiente

Acta en `docs/revisiones/claude-fase4-daily--pasada-1.md`, `sha256` `5ed2a35325bf8898688ee26a8eaae14763e827dcf956e3423f7c9bb9438359a2`.

**7,5 min de 10, 16 mutaciones, 3 bloqueantes, 3 relevantes, 2 menores.** Orden 3-4-5-9 → 8 → 1-2-10 → 6-7; **declara sin recorrer** la categoría 11, y parcialmente la 2 y la 6.

### Bloqueante 1 — OPSEC: la clave podía llegar a un repositorio público

**Comprobado, no supuesto.** Con la clave terminada en salto de línea —lo que produce cualquier copiado descuidado en GitHub Secrets— `urllib` lanza `ValueError: Invalid header value b'LA_CLAVE\n'`. Ese texto acaba en `motivo_fallo`, y `recoleccion.json` lo lleva al commit del workflow diario. El `::add-mask::` protege el log; **no protege el fichero**.

La redacción va en la **frontera de persistencia** y no solo donde hoy se sabe que el secreto se cuela: los mensajes de excepción de terceros no son un contrato. Se declara con una marca visible en vez de borrar, y por debajo de ocho caracteres no se sustituye nada.

### Bloqueante 2 — el informe del fallo total no se commiteaba nunca

`git add a b c` con una ruta ausente aborta el índice **entero**, y el `|| true` lo convertía en «sin cambios que commitear» **en verde**. El caso en que falta una ruta es precisamente el fallo total, donde el pipeline no escribe el estado para no corromper el diferencial (§14.3): el informe que el `always()` existe para publicar se perdía justo el día en que más falta hace.

### Bloqueante 3 — el workflow habría muerto todos los días

El paso que lee el pin usaba PyYAML **dos pasos antes** de instalar el paquete. Y el paso final habría atribuido el rojo a un «fallo total de recolección» que nunca ocurrió.

### Relevantes cerrados

El arnés usaba pertenencia de subcadena donde la propiedad es universal, y siete mutaciones sobrevivían. Ahora: ruta de caché **atada a la que construye el cargador**, sin `restore-keys` —serviría el bundle de otro pin—, sin push forzado, y sin caminos por los que el secreto se imprima.

## Anotado, no corregido

`CLAUDE.md` está congelado hasta el cierre de fase, así que §11.2 sigue diciendo «pendiente de implementación» y §9 sigue sin enumerar los validadores. Las dos discrepancias van a `docs/proceso-pendiente.md`.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
