# Pull request #13 — Fase 4 (1/4) — Cierre de fase en §13, recuento por severidad y los cuatro relevantes de la fase 3

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T08:44:14Z |
| **Fusionado** | 2026-08-02T08:54:56Z |
| **Commit de fusión** | `9f25daa9c53cf6cd159dde324991c90b72fa76cf` |
| **Rama** | `claude/fase4-cierre-y-pendientes` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/13 |
| **Comentarios archivados** | 3 |

## Descripción

Primer paso de la fase 4. Define qué significa cerrar una fase, añade una obligación al protocolo y salda los cuatro relevantes que la fase 3 dejó abiertos.

## Cierre de fase (§13)

Dos mecanismos dependían de saber cuándo se cierra una fase —la regla de retirada del registro de métricas, sobre todo— sin que estuviera definido en ninguna parte: `grep -rn "fase 4"` solo encontraba los textos que lo invocaban, ninguno que lo definiera. Era la alarma que no puede dispararse, señalada como H-6 en la revisión del PR #12.

**§13 pasa a declarar que el cierre de la fase 4 y la versión 1 son el mismo hito, no dos.** La versión 1 es lo que la fase 4 produce; separarlos permitiría dar una fase por cerrada con la versión a medias. Dos de los seis puntos se afilan, y por el mismo motivo en ambos casos:

- El punto 4 exige un informe **publicado** en `reports/` por el workflow diario, no un workflow en verde. Un workflow verde demuestra que el proceso no falló; un informe demuestra que produjo algo — §14.3 aplicado al criterio de terminado.
- El punto 3 exige cobertura de **los tres modos de informe**, no «los tests pasan»: una batería en verde sobre dos de tres también pasa.

## Recuento por severidad en todo informe de revisión

Nueva obligación de la salida esperada del revisor, con la evidencia y **su artefacto**: de las siete pasadas de la fase 3, cuatro publicaron informe, y el único **sin línea de recuento** —la pasada 4 del PR #11— es el único cuya fila del registro se anotó mal. El artefacto que lo demuestra es el hilo del PR #12 (hallazgo H-1 y su respuesta), **no** el historial de git: la corrección y el error convivieron dentro de ese PR y el *squash* dejó en `main` un solo commit con el valor corregido. Quien lo audite con `git log -p` no lo encontrará, y esa advertencia va escrita — es la regla 6 aplicada a la propia evidencia.

## Los cuatro relevantes pendientes de la fase 3

Los cuatro **verificados por mutación**, no por lectura.

- **R-2, el del arnés.** `returncode == 1` no distinguía «contrato roto» de «el script reventó»: un `TypeError` en la cola de `verificar_bundle_attack` pasaba por el rojo esperado con la batería entera en verde. Reproducido antes de tocar nada. Ahora se exige la línea de resumen —que solo se imprime si el script llegó a **decidir**— y la ausencia de traza.
- **R-3.** Las comprobaciones de forma que §11.3 exige no se ejercitaban en su dirección de fallo. Nuevo modo `sin_forma` del arnés. De paso apareció que §11.3 nombra **tres** y el código tenía dos, con el docstring prometiendo tres: se añade la tercera, contrastada contra la magnitud `objetos_revocados_o_deprecados` que la línea base declaraba **sin ningún consumidor** desde que se fijó el pin.
- **R-4.** La comprobación de OPSEC no alcanzaba al interior del transporte, que es donde la cabecera se une a la petición. El arnés pasa a sustituir el **transporte** (`_abrir_urllib`) en lugar de `solicitar`, de modo que el cliente real construye la petición. Verificado en las dos direcciones: reconstruido el arnés anterior, una fuga inyectada en `collect/base.py` **no** se detectaba; con el nuevo, sí.
- **M-1.** El test se acoplaba a `línea base 821`, valor que §5.5 obliga a remedir al subir el pin: ese día habría fallado con el diagnóstico falso «la barrera de recuentos no llegó a ejecutarse». Se lee de `config/attack_bundle.yaml`.

Además, el desenlace «contrato roto» de una **fuente** —que tampoco se ejercitaba— pasa a tener su propio modo, con el bundle deliberadamente no servido para que el código de salida lo decida solo la fuente.

## Revisión

**Tres pasadas independientes, todas sin bloqueantes**; el ciclo cierra por la regla 7. Los informes y las respuestas van en los comentarios.

Las pasadas 2 y 3 encontraron que **dos correcciones de la pasada anterior habían introducido un defecto nuevo**: la protección contra una configuración malformada dejaba fuera del `try` las dos líneas que la usan —un bloque vacío da `None`, y `None.get` es `AttributeError`—, y la tercera comprobación de forma medía «no hay objetos retirados» en vez de «los marcadores siguen existiendo», dando un rojo falso en un caso y pasando en verde en el otro. Ambos corregidos y verificados.

**193 tests en verde**, `ruff` limpio, `--sin-red` en 0.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 3 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

---

**1. @vigiabref · 2026-08-02T08:44:47Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/13#issuecomment-5156625197 -->

# Revisiones independientes — pasadas 1 y 2

> **Procedencia.** Los informes los produjeron dos sesiones de revisión independientes antes de abrirse el PR, y los transcribe la sesión implementadora porque la revisora se ejecuta sin permiso de escritura. Se reproducen sus hallazgos, sus recuentos y sus limitaciones; el texto completo de cada uno está en la sesión que lo generó.

---

## Pasada 1 — sobre el diff completo de la rama

**Artefactos contra los que ejecutó:** copia del repo en `dfdad9a` con `PYTHONPATH` confirmado; suite (191 pasan); `ruff format --check`; 6 mutaciones de excepción para R-2; neutralización de las 5 ramas de `verificar_bundle_attack` para R-3; **7 inyecciones de fuga de la clave en `collect/base.py` más la reconstrucción del arnés anterior** para R-4; 3 mutaciones de `config/attack_bundle.yaml` para M-1; `coverage` (79% de sentencias); comentarios reales del PR #11 por la API de GitHub; `git grep "fase 4"` en `main` y en `HEAD`.

**Verificó como cerrados, reproduciéndolos:** R-2 (los cinco `TypeError` inyectados matan el test nuevo; con la aserción anterior tres pasaban), R-3 (neutralizar ambas ramas falla solo el test nuevo), **R-4 en las dos direcciones** —con el arnés anterior la fuga daba `1 passed`, con el nuevo falla— y M-1.

**Relevantes (6):**
- **R-A** — §11.3 nombra tres comprobaciones de forma y el código tenía dos; los marcadores `revoked`/`x_mitre_deprecated` solo se usan, nunca se contrastan.
- **R-B** — el contrato roto de una **fuente** nunca se ejercita en su dirección de fallo; `:290`, `:294` y `:333-337` sin ejecutar.
- **R-C** — un fallo de configuración propia mata el script con traceback, en contra del criterio que el propio fichero escribe cuatro líneas más arriba.
- **R-D** — la evidencia de la regla de recuento no es reproducible, y el registro la contradice.
- **R-E** — el disparo de la retirada pasa de indefinido a *puede no llegar nunca*: el punto 4 exige un `daily.yml` que no existe, sin fecha tope ni desenlace alternativo.
- **R-F** — la alarma de deriva del pin no se puede demostrar en ninguna de sus dos direcciones; `:343` y `:514-519` sin ejecutar.

**Menores (9):** M-a (alcance de OPSEC sobreafirmado — medido: 4 de 7 fugas detectadas), M-b (`_linea_base` cambia fragilidad por ceguera), M-c (aserción sensible al orden con diagnóstico invertido), M-d (§15 desactualizado), M-e (`ARNES_BUNDLE` sin diagnóstico), M-f (la entrada 18 atribuye a §2 una dependencia que no tiene), M-g (dos de los seis puntos de §13 no son mecánicamente comprobables), M-h, M-i (la regla de recuento no se aplica la categoría 9).

**No pudo verificar:** ninguna fuente viva; los valores de la línea base; los tres informes no publicados del PR #11; la matriz de CI; el comportamiento de `logging` en el runner.

**Recuento: 0 bloqueantes, 6 relevantes, 9 menores.**

---

## Pasada 2 — acotada a las correcciones (`b37035e`)

**Artefactos:** copia en `b37035e`; suite (192 pasan); `coverage` **85%** frente al 79% anterior; 4 malformaciones de `config/attack_bundle.yaml` ejecutadas contra el script; arnés propio con bundle legítimo y línea base remedida para medir el falso positivo; 4 mutaciones sobre `verificar_fuente`/`main`.

**Verificación positiva sobre R-C:** convertir el fallo de configuración en «no verificable» crea el extremo simétrico —el workflow semanal en verde con nuestra propia línea base rota— y **ese extremo está cubierto** por `--sin-red` en CI.

**Relevantes (4):**
- **R2-1** — la corrección de R-C no cubre la malformación más probable: `pin.get(...)` y `base.get(...)` quedaron **fuera** del `try`, y un bloque presente y vacío da `None`, cuyo `.get` es `AttributeError`. Dos de cuatro malformaciones siguen reventando con traza.
- **R2-2** — la tercera comprobación de forma mide una magnitud distinta de la que §11.3 nombra, y falla en las dos direcciones: **falso positivo** con un bundle legítimo sin retirados —«un rojo que no puede ser cierto»— y **zona ciega** si desaparece uno solo de los dos marcadores. La pieza correcta ya estaba en el repositorio: `objetos_revocados_o_deprecados: 3`, declarada y sin ningún consumidor.
- **R2-3** — el test de `fuente_rota` no puede demostrar su tesis: el rojo está sobredeterminado y la mutación «el código de salida solo lo decide el bundle» **sobrevive a los 192 tests**, incluido el test que lleva ese nombre.
- **R2-4** — el párrafo reescrito por R-D introduce «cuatro informes publicados» sin citar artefacto y contra el único registro que el repositorio conserva.

**Menores (6):** R2-5 (R-C es la única corrección sin test que la ejercite — un test le habría encontrado R2-1 en el acto), R2-6 (comentarios que cuentan dos donde hay tres), R2-7 (el asunto del commit dice «cierra seis relevantes» y documenta dos sin corregir), R2-8 (la mitad «formato temporal» de `verificar_fuente` sigue sin recorrerse), R2-9 (la excepción operativa de quién anota la fila vive en el documento que §9.1 declara sin autoridad), R2-10 (la entrada 18 se reescribe en vez de añadir, contra §9.1).

**No pudo verificar:** el bundle vivo (todo R2-2 medido sobre bundles sintéticos); los contratos vivos; el hilo del PR #12; la homologación del 79% anterior; GitHub Actions.

**Recuento: 0 bloqueantes, 4 relevantes, 6 menores.**

---
_Generated by [Claude Code](https://claude.ai/code)_

---

**2. @vigiabref · 2026-08-02T08:45:21Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/13#issuecomment-5156628247 -->

# Revisión independiente — pasada 3, acotada a `21e3798`

> Transcrito por la sesión implementadora; el revisor se ejecuta sin permiso de escritura.

Sesión distinta de la implementadora, sin su contexto. Repositorio original intacto (`git status` limpio en `21e3798`); todo sobre copias en el scratchpad. **Todo lo que sigue se ejecutó; nada se concluyó leyendo.**

**Artefactos:** copia en `21e3798` con `PYTHONPATH` confirmado (`threatintel.__file__` apunta a la copia); copia del padre `b37035e` para comparar cobertura; cuatro mini-repos con `scripts/` + `config/` + `tests/fixtures/` propios para **15 malformaciones** de `config/attack_bundle.yaml`; arnés propio con bundle conforme y línea base remedida. Batería: **193 pasan en 4,29 s**; `ruff` limpio.

## Categorías

**1.** Conjetura presentada como verificación — R3-2, R3-4. **2.** Contrato externo — sin hallazgos; los contratos vivos siguen sin verificar desde aquí. **3.** Validez sintáctica con sentido incorrecto — R3-3. **4.** Alarma degenerada — R3-1, R3-3, R3-4, M-6. **5.** Requisito no satisfecho — sin hallazgos; la comprobación de insumos no aplica a estas líneas. **6.** Coste — sin hallazgos: los tres modos suman 0,84 s, el test nuevo 1,06 s. **7.** Deriva — M-1 a M-5. **8.** OPSEC — sin hallazgos: la config sintética se escribe en `tmp_path`, nunca sobre la real. **9.** Simetría — R3-2, R3-3, M-6. **10.** Defecto introducido por una corrección — R3-1 a R3-5, M-1.

## Relevantes

**R3-1 — `_bloques_del_pin()` sigue reventando ante la malformación que este proyecto tiene más papeletas de producir.** `read_text(encoding="utf-8")` lanza `UnicodeDecodeError`, que es subclase de `ValueError`, **no de `OSError`**, y escapa a la captura. De 15 malformaciones, catorce se declaran y una revienta. En el camino de producción no se imprime el resumen ni la declaración del bundle, y el workflow queda rojo indistinguible de un contrato roto. No es teórico: el fichero está lleno de comentarios en español con acentos y basta un editor que guarde en latin-1.

**R3-2 — R2-8 reintroduce un nivel más abajo la sobredeterminación que R2-3 cerró.** El modo `fuente_rota` rompe a la vez el nombre y el formato, de modo que `ausentes` ya basta para el rojo. La mutación `return ausentes | formato → return ausentes` **sobrevive a los 193 tests**. El test exige el mensaje del formato, pero ese mensaje se imprime **antes** del `return`: acredita que la rama se recorre, no que su resultado cuente.

**R3-3 — La tercera comprobación de forma dejó de ser una comprobación de forma.** `objetos_revocados_o_deprecados` se calcula como `len(software) - len(vivos)`, y la línea base es internamente consistente (`824 − 821 = 3`), de modo que la condición implica que la barrera de recuentos **ya disparó**. Verificado ejecutando y neutralizándola: el desenlace no cambia. No aporta detección, y su mensaje afirma una causa que la condición no establece — dispara igual cuando MITRE deprecia un objeto más, donde lo cierto es lo contrario. A favor: el rojo falso **sí desapareció**, verificado con línea base 0 sobre un bundle legítimo.

**R3-4 — Nada en la batería demuestra que el verificador pueda terminar *sin* defectos.** Los tres modos sirven bundles que fallan el digest, o no sirven bundle; `digest coincide con el pin` y `contrato intacto` **nunca se ejecutan**. La tesis central de R2-2 la comprobó el revisor con arnés propio (y se cumple: `rc=0`, «Todos los contratos verificados»), no la batería.

**R3-5 — El test que cierra R2-1 no fija dos de las cinco excepciones.** Estrechar la captura a `(OSError, yaml.YAMLError, KeyError)` sobrevive a los 193 tests: ninguna de las cuatro variantes produce `TypeError` ni `AttributeError`. Las tres primeras funcionan hoy, pero un futuro estrechamiento las rompería en verde.

## Menores

**M-1** el protocolo dice «las dos primeras aplicaciones» cuando van cuatro, y lo lleva mal el documento **normativo** — el defecto que R2-6 cerraba, reaparecido en la prosa que lo cerraba. **M-2** «todas las filas de revisores llevan †» es falso contra su propia tabla (3 de 12). **M-3** `BUNDLES["fuente_rota"]` quedó muerto; «tres cuerpos» son dos cuerpos y tres modos. **M-4** la invariante que promete el comentario de `MAGNITUDES_LINEA_BASE` vale para seis de ocho. **M-5** `AttributeError` en la captura es hoy inalcanzable; la pieza portante es la guarda `isinstance`. **M-6** `comprobar_sin_red` da por completa una línea base con magnitudes no numéricas (`objetos_totales: 'muchos'` → sale 0, y en producción daría un rojo falso). **M-7** el arnés lanza `ErrorRed` desde el transporte, cortocircuitando el bucle de reintentos de §14.2. **M-8** la cobertura **no se movió** (85% antes y después): el test nuevo ejecuta una copia del script en `tmp_path`, que ninguna herramienta atribuye a `scripts/`.

**Ramas sin ejercitar:** salida verde del bundle (507, 573); aviso de deriva del pin (565-570); `commit_sha` nulo y línea base incompleta en producción; cuerpos no interpretables de las tres fuentes; `no_result` y `query_status` distinto de `ok`; ramas de error de `comprobar_sin_red`; `LookupError` del AST.

## Lo que no pudo verificar

Ningún contrato vivo. Los valores reales de la línea base (el argumento algebraico de R3-3 no depende de que sean ciertos, solo de que `824−821=3`, verificado sobre el fichero). Python 3.12. El workflow semanal. El hilo del PR #12 (sin red). Los `~35 min` de la fila anterior. Cobertura de **ramas**: midió sentencias, no ramas.

## Veredicto y recuento

**0 bloqueantes** — «no he forzado ninguno: el código se comporta correctamente ante toda entrada realista salvo una config no-UTF-8, y ese fallo es visible, propio y recuperable, no una laguna silenciosa». Las dos correcciones que el commit se proponía cerrar **funcionan**, verificadas por mutación y con arnés propio.

| Severidad | Nº | Identificadores |
|---|---|---|
| **Bloqueantes** | **0** | — |
| **Relevantes** | **5** | R3-1, R3-2, R3-3, R3-4, R3-5 |
| **Menores** | **8** | M-1 … M-8 |

---
_Generated by [Claude Code](https://claude.ai/code)_

---

**3. @vigiabref · 2026-08-02T08:45:53Z · comentario**

<!-- original: https://github.com/vigiabref/threat-intel-pipeline/pull/13#issuecomment-5156631258 -->

## Respuesta de la sesión implementadora — las tres pasadas

Tres pasadas, **ninguna con bloqueantes**. El ciclo cierra por la regla 7. Corregido lo que las pasadas 1 y 2 señalaron; de la pasada 3 solo las dos cifras falsas, y explico por qué el resto **no** se corrige aquí.

### Lo aceptado y cerrado

**Pasada 1.** Los seis relevantes y siete de los nueve menores. Los tres de fondo eran R-A (§11.3 nombra tres comprobaciones de forma y había dos, con el docstring prometiendo tres), R-B (el contrato roto de una *fuente* no se ejercitaba) y R-C (un fallo de configuración propia mataba el script con traza, contra el criterio escrito cuatro líneas más arriba). R-D fue el más incómodo y lo verifiqué contra el historial: la fila **sí** estuvo mal —`1 | 5 | 1`—, pero el *squash* del PR #12 borró la historia intermedia de `main`, de modo que el revisor tenía razón en que su evidencia no era localizable. El párrafo pasa a citar el hilo del PR, y la advertencia queda escrita como regla general.

**Pasada 2.** Los cuatro relevantes y cuatro menores. **Dos de ellos eran defectos que mi corrección anterior había introducido**, y ambos merecen nombrarse:

- **R2-1**: puse el `try` alrededor de la lectura del YAML y dejé fuera las dos líneas que usan los bloques, dos filas más abajo. Un bloque presente y vacío da `None`, y `None.get` es `AttributeError`. Cerré la puerta por la que creía que entraba el fallo y dejé abierta la de al lado.
- **R2-2**: mi tercera comprobación medía «no hay objetos retirados» cuando §11.3 pide «los marcadores siguen existiendo». Daba un **rojo falso** —el mecanismo de fatiga que la distinción roto/no-verificado existe para evitar— y pasaba en verde si desaparecía uno solo de los dos marcadores. Lo notable: la pieza correcta ya estaba en el repositorio, `objetos_revocados_o_deprecados: 3`, declarada desde que se fijó el pin y **sin ningún consumidor**.

### Lo corregido de la pasada 3

Solo **M-1** y **M-2**, porque eran cifras falsas contra su propio artefacto: el protocolo decía «las dos primeras aplicaciones» cuando van cuatro —y lo llevaba mal el documento normativo—, y el registro afirmaba «todas las filas de revisores llevan †» cuando 3 de 12 lo llevan por ese motivo.

### Lo que NO se corrige aquí, y por qué

Los cinco relevantes y seis menores restantes de la pasada 3 quedan documentados sin corregir. No es desdén por los hallazgos —**R3-3 es el más agudo de las tres pasadas**: demuestra algebraicamente que mi tercera comprobación no aporta detección, porque es una combinación lineal de dos magnitudes que la barrera de recuentos ya contrasta—. Es una decisión de proceso:

1. **La regla 7 lo permite**: el ciclo cierra sin bloqueantes, y los relevantes se documentan y no bloquean.
2. **La categoría 10 lo desaconseja**: sería la cuarta corrección consecutiva sobre el mismo fichero, y de las últimas ocho de esta sesión **siete introdujeron un defecto nuevo**. Las dos anteriores lo hicieron, y las encontró la pasada siguiente. Colgar una cuarta tanda del final de un ciclo ya cerrado es exactamente el patrón que la categoría describe.
3. **Merecen su propio cambio con su propia revisión.** R3-1 (`UnicodeDecodeError` no es subclase de `OSError`) y R3-3 (la comprobación redundante) son correcciones de una y de pocas líneas, pero cada una necesita el arnés que la demuestre — y R3-4 dice justamente que hoy **nada** demuestra que el verificador pueda terminar sin defectos.

Quedan enunciados para su cambio: **R3-1** (config no-UTF-8 revienta con traza), **R3-2** (el modo `fuente_rota` sobredetermina de nuevo el rojo: hace falta un cuarto modo con solo el formato roto), **R3-3** (contrastar contra la línea base la convierte en dependiente de la barrera; una sonda independiente exigiría mirar los marcadores, no contarlos), **R3-4** (falta un modo con bundle conforme, que de paso cerraría R-F), **R3-5** (dos de las cinco excepciones sin fijar), **M-3** a **M-8**.

Y dos que necesitan **decisión del mantenedor**, no parche:

- **R-E** — al anclar la retirada del registro a §13, el instante pasa de *indefinido* a *preciso pero condicionado a un evento que puede no llegar*: el punto 4 exige un informe publicado por un `daily.yml` que aún no existe. Antes era vago pero siempre podía ocurrir.
- **R3-3** — si un pin futuro se remide sobre un catálogo con 0 retirados, la comprobación pasa a ser zona ciega para el fallo que nombra.

### Estado

**193 tests en verde**, `ruff check` y `ruff format --check` limpios, `--sin-red` en 0.

---
_Generated by [Claude Code](https://claude.ai/code)_
