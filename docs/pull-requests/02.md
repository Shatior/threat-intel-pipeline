# Pull request #2 — Herramienta de captura de fixtures (workflow manual)

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-01T17:12:44Z |
| **Fusionado** | 2026-08-01T17:12:51Z |
| **Commit de fusión** | `c8c3380f9f2410809ce303ac884e044955a20bd7` |
| **Rama** | `claude/tooling-capturar-fixtures` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/2 |
| **Comentarios archivados** | 0 |

## Descripción

## Qué es

Incorpora **únicamente** `.github/workflows/capturar-fixtures.yml` a `main`. Es una **herramienta de operación, no parte de la fase 2 de colectores**.

El workflow es manual (`workflow_dispatch`): consulta ThreatFox (con el secreto `ABUSECH_AUTH_KEY`, `days: 1`) y descarga el feed de CISA KEV, verifica el estado de aplicación de ThreatFox, reduce cada respuesta a unos pocos registros representativos (con un registro malformado en ThreatFox para §14.4), escribe `tests/fixtures/` con un README y abre un pull request. Permisos mínimos y acciones de terceros fijadas por hash.

## Por qué en su propio PR, separado de la fase 2

GitHub solo permite **disparar workflows `workflow_dispatch` desde la rama por defecto**. Para poder ejecutar la captura hace falta que el fichero esté en `main`. Este PR lo lleva allí **sin arrastrar el código de la fase 2** (colectores, esquema ampliado, etc.), que llegará en su propio PR. No añade código del pipeline ni dependencias.

## Verificación

- No modifica código del pipeline: el único fichero es el workflow.
- La clave nunca se imprime ni se escribe en ficheros; se comprueba explícitamente antes de abrir el PR.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
