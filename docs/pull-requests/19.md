# Pull request #19 — Bloque 4 de la fase 4: el renderizado del informe de §8 en los tres modos

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-02T21:24:21Z |
| **Fusionado** | 2026-08-02T22:07:56Z |
| **Commit de fusión** | `053b0c4b2dc6b97a0795bb682d5a4ee2825f48bc` |
| **Rama** | `claude/fase4-informe` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/19 |
| **Comentarios archivados** | 0 |

## Descripción

## Qué entra

El **renderizado del informe de §8**: las ocho secciones, en su orden, en los tres modos de §6.2. Y el cableado de `run`, que cierra el punto 1 de §13 — el ciclo completo hasta el informe en una sola invocación, publicando `reports/YYYY/YYYY-MM-DD.md` y `reports/latest.md`.

**386 tests en verde**, `ruff` limpio, `verificar_contratos.py --sin-red` en 0.

### El renderizador no calcula, recibe

`ContextoInforme` llega con las magnitudes ya computadas por §6 y §5. Es lo que impide que una cifra del informe tenga aquí una segunda implementación distinta de la de su sección, y lo que permite comprobar las reglas de §8.3 sobre el texto sin montar un pipeline entero.

### Las reglas de §8.3, como comprobaciones sobre el texto renderizado

No sobre la plantilla: una comprobación satisfecha leyendo el código diría que la plantilla es correcta, no que el informe lo sea.

- **Vocabulario reservado** acotado a las secciones 2 a 7, con el alcance exacto de §6.2 — la declaración de lo suprimido **nombra** los cálculos que no publica, y eso no es una infracción.
- **Frase canónica del denominador** de §8.1, literal: «De las **47 familias observadas**, **12** tienen entrada en ATT&CK» y «8 de las 47 familias observadas (17%)». Nunca «de ellas».
- **Técnicas inferidas suprimidas y declaradas** en línea base; las derivadas se publican igual, porque su denominador es un agregado deslizante y no un diferencial.
- **Cola acotada a su cabecera** con el total y el denominador nombrados.
- **Nada se publica vacío**: o con contenido, o suprimido con su motivo.
- **Fallo total** reducido a la declaración del fallo, sin juicios ni recomendaciones.

## Lo que encontró leer el informe generado, y no los tests

Tres defectos con los 377 tests en verde:

1. **El BLUF del diferencial imprimía la línea de la cabecera en bruto** cuando los intervalos difieren entre fuentes. Reaprovechar el texto de la cabecera parecía ahorro: la cabecera es una ficha con viñetas y el BLUF es prosa.
2. La prosa **no concordaba en número** («1 reaparecidos»).
3. Las duraciones **anidaban paréntesis** («(6 días (144 h))»).

Los tres tienen prueba y las tres mutaciones mueren.

## Cinco correcciones de producto, del mantenedor leyendo los informes como destinatario

1. **Fuera los punteros a secciones** del informe renderizado. El lector no tiene la especificación; la justificación se queda, el puntero se va. Alcanza también a los motivos que redacta `analyze/diff.py`, que viajan al informe por la obligación de declarar lo no publicado — y hay un test que lo cubre por el camino real, no solo con motivos sintéticos.
2. **Los BLUF abren con lo accionable** —las vulnerabilidades con plazo próximo— y dejan la declaración metodológica después. La declaración de modo sigue siendo obligatoria; solo cambia de sitio.
3. **Curar la tabla de vectores sale de la sección 7** y pasa a la nota metodológica: es mantenimiento del pipeline, no una recomendación de seguridad, y mezclarlas le pedía al lector decidir cuál atiende primero cuando no compiten por el mismo tiempo ni las ejecuta la misma persona.
4. **Los indicadores destacados excluyen `vulnerability`**: ya tienen la sección 4, y aquí solo desplazaban a la infraestructura accionable.
5. **Lista explícita y cerrada de hashes triviales** que nunca se publican, empezando por el digest del fichero vacío en MD5, SHA-1 y SHA-256. Coinciden con todo fichero vacío y no señalan nada. **No se descartan en silencio**: el recuento se declara junto a la tabla, porque un indicador que desaparece sin nota es indistinguible de uno que no se observó.

## De paso

Se elimina la clave duplicada `ventana_dias_vencimiento`, que vivía anidada bajo `informe:` y sin leerse. Dos sitios con la misma magnitud es lo que este proyecto no admite en ninguna otra parte.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
