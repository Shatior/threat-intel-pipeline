# Pull request #21 — Corrige el orden de la sección 4 y la atribución por fuente del BLUF

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-03T02:14:12Z |
| **Fusionado** | 2026-08-03T02:22:30Z |
| **Commit de fusión** | `1793e9ba18b0f7f9115b99810e531722af76cbe6` |
| **Rama** | `claude/fase4-correcciones-informe` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/21 |
| **Comentarios archivados** | 0 |

## Descripción

Dos correcciones de producto encontradas **leyendo el informe real** publicado en `main` el 2026-08-02, no por un test. Ninguna de las dos hacía fallar nada.

## 1. La sección 4 contradecía a su propia recomendación

El orden era «uso en ransomware primero, fecha límite después». Medido contra el catálogo real: **1.654 de las 1.656 entradas ya tienen el plazo vencido** —CISA lo fija unas tres semanas después del alta—, de modo que «fecha límite más próxima» sobre el catálogo entero es en realidad un orden por antigüedad. La cabecera publicada salió entera con plazo `2021-11-17`, y el CVE que vencía esa semana —el que el BLUF y las recomendaciones mandaban parchear— no aparecía en ninguna fila.

El orden pasa a ser:

1. lo que **aún no ha vencido**, de lo que vence antes a lo que vence después;
2. lo **vencido**, de lo más recientemente vencido a lo más antiguo;
3. lo que no declara plazo legible, al final —nunca intercalado;
4. y a igualdad de plazo, el uso conocido en ransomware, después el CVE.

Además, **las entradas con plazo en los próximos 7 días se publican siempre**, aunque el recorte de la cabecera las dejara fuera. Con el orden nuevo caen en cabecera por construcción, así que la unión no debería añadir nada; se escribe igualmente porque la garantía es del producto y no del orden.

La regla se escribe **una sola vez** y la comparten la sección 4 y la cola de trabajo.

Cabecera resultante sobre el estado real:

| CVE | plazo | producto |
|---|---|---|
| `CVE-2026-60137` | 2026-08-04 | Core |
| `CVE-2025-68686` | 2026-08-10 | FortiOS |
| `CVE-2026-20316` | 2026-08-01 | Secure Firewall Management Center |
| `CVE-2026-16812` | 2026-07-30 | VeloCloud Orchestrator |

## 2. El BLUF atribuía a una fuente el total de todas

Publicó «**7368 indicadores** en `cisa-kev`» cuando 1.656 eran de KEV y el resto de ThreatFox. Un número correcto en su magnitud y falso en su sujeto no se detecta releyendo el número: hay que ir a buscar de dónde sale. Ahora declara el reparto real por fuente.

Y arrastraba un segundo defecto que el primero tapaba: **contaba en el censo la parte de una fuente que no alcanzó `correcta`**, que la especificación excluye de los recuentos por fuente, tipo y familia. El mismo informe declaraba en su sección 5 que el panorama de esa fuente no se publicaba, de modo que el BLUF afirmaba sobre una recolección que el cuerpo declaraba no publicable. El censo se calcula ahora solo sobre las fuentes en estado `correcta` y declara cuáles quedaron fuera y por qué.

El recuento de familias tenía el mismo defecto un campo más allá —anunciaba «89 familias» mientras la sección 5 declaraba el panorama no disponible— y se corrige con la misma condición que ya gobierna esa sección.

## Código muerto retirado

`enrich.attack.cola_de_trabajo` era una **segunda implementación del mismo orden** que ningún camino de producción invocaba, con tres tests en verde sobre ella. La cola que llega al informe la ordena `cli._cola_sin_clasificar`. Es el mismo defecto que la revisión del bloque 3 encontró en la marca de agua, con otra ropa: dos copias de una regla divergen, y la divergencia no falla, solo miente. La función se elimina y esa cobertura apunta ahora a la cola que se publica.

## Verificación

**431 tests en verde**, `ruff check` y `ruff format --check` limpios.

Verificado por mutación —cinco mutaciones, cada una muerta por el test que le toca:

| Mutación | Test que la mata |
|---|---|
| Orden ascendente puro (lo vencido delante) | `test_la_seccion_4_ordena_por_fecha_limite_y_no_por_antiguedad` |
| Sin la garantía de inclusión de plazo próximo | `test_las_entradas_de_plazo_proximo_se_publican_aunque_el_recorte_las_deje_fuera` |
| Total de todas atribuido a una sola fuente | `test_el_bluf_declara_el_reparto_por_fuente_y_no_atribuye_el_total_a_una` |
| Familias publicadas sin panorama publicable | `test_el_bluf_no_publica_familias_si_el_panorama_no_es_publicable` |
| Desempate por ransomware anulado | `test_el_uso_en_ransomware_desempata_a_igualdad_de_plazo` |

**Laguna declarada: estas correcciones no han pasado por revisión independiente.** No se encargó pasada para ellas. Están verificadas por mutación por la sesión implementadora, que es exactamente la clase de evidencia que el protocolo del proyecto declara insuficiente por sí sola.

## Discrepancia con `CLAUDE.md`, que sigue congelado

§5.2 y §8.3 describen el orden anterior —«primero las de `knownRansomwareCampaignUse` conocido, después las de `dueDate` más próximo»—. Esta rama **no toca la especificación**, conforme al régimen de la fase 4. Queda anotada para decidirla en el bloque 6 junto al resto de discrepancias acumuladas.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
