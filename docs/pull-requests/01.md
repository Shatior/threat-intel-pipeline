# Pull request #1 — Base del pipeline: estructura, esquema §4, colector base, config, CLI y tests

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-01T04:42:00Z |
| **Fusionado** | 2026-08-01T06:06:26Z |
| **Commit de fusión** | `96ad5d439cf785515768666d1ed5701becb06181` |
| **Rama** | `claude/base-proyecto-colector-fadit9` → `main` |
| **URL original** | https://github.com/vigiabref/threat-intel-pipeline/pull/1 |
| **Comentarios archivados** | 0 |

## Descripción

## Contexto

Primera fase del proyecto: la **base** del pipeline CTI conforme a `CLAUDE.md`. No incluye colectores, enriquecimiento ni informe (llegarán en fases posteriores, con su propio PR).

## Qué incluye

- **Estructura del repositorio (§9)**: paquetes de `collect/`, `normalize/`, `enrich/`, `analyze/`, `report/` (los tres últimos como paquetes vacíos para fases futuras), más `config/`, `data/state`, `data/cache`, `reports/`, `tests/` y `scripts/`.
- **Esquema de normalización §4 con validación** (`src/threatintel/normalize/schema.py`, Pydantic v2): modelo `Indicador` con los campos §4, enums de `type`/`source`/`tlp`/mapeo, marcas temporales forzadas a UTC, `confidence` 0–100, normalización de `value` (minúsculas, dominios sin punto final, defang revertido) y mapeo a ATT&CK con `rationale` obligatorio (§5).
- **Doble identidad determinista**: `id` = `sha256(type + value + source)` (identidad de registro) y `clave_canonica` = `sha256(type + value)` (identidad de indicador, independiente de la fuente). La consolidación entre fuentes se basa en `clave_canonica` (§6).
- **Interfaz base de colector** (`src/threatintel/collect/base.py`): ABC `ColectorBase` con `recolectar()` y `recolectar_seguro()`, que captura fallos de fuente sin abortar el pipeline (§10).
- **Configuración y CLI**: `config/*.yaml` + `config.py` (carga YAML, `LOG_LEVEL`, logging estructurado) y `python -m threatintel run` como comando **no-op** que declara que el pipeline aún no está implementado.
- **Andamiaje**: `pyproject.toml` (manifiesto único de dependencias, §9/§10), `.env.example`, `README`, hook de `SessionStart`.

## Correcciones incluidas

- Se añade `clave_canonica` y se actualizan §4 y §6 de `CLAUDE.md` para distinguir identidad de registro frente a identidad de indicador.
- §9 de `CLAUDE.md` refleja `pyproject.toml` en lugar de `requirements.txt`; se elimina `requirements.txt`.

## Verificación

- `pip install -e ".[dev]"` ✓
- `pytest` → **42 passed** ✓
- `python -m threatintel run` → sale con código 0 y log en español ✓
- Sin secretos versionados; `.env` y `data/cache/` ignorados ✓

## Fuera de alcance

Colectores, enriquecimiento (MITRE ATT&CK), análisis/diferencial, informe y la automatización `daily.yml` (§11) — cada uno en su fase y PR.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
